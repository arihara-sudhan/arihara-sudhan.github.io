SOURCE: Elmasri & Navathe – “Database System Concepts” by Abraham Silberschatz, Henry F. Korth and S. Sudarshan.
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[07/11/2025]
1. Database-Management System (DBMS)
𖦹 A DBMS is a collection of interrelated data and programs to access the data.
𖦹 The collection of data is called the database and contains information relevant to an enterprise.
𖦹 Main goal: Store and retrieve information conveniently and efficiently.

2. Purpose of Database Systems
𖦹 Used to manage large amounts of information.
𖦹 Involves defining storage structures and providing methods to manipulate data.
𖦹 Must ensure safety of data even during system crashes or unauthorized access attempts.
𖦹 Must prevent anomalies when data is shared by multiple users.

3. Importance of DBMS Concepts
𖦹 Information is vital in organizations.
𖦹 Computer scientists developed many concepts and techniques for effective data management.
𖦹 These concepts form the foundation of database systems.

4. Representative Applications of Database Systems
𖦹 Enterprise Information: Sales, accounting, human resources.
𖦹 Manufacturing: Supply chain, production tracking, inventory.
𖦹 Banking and Finance: Accounts, loans, transactions, stock trading.
𖦹 Universities: Student info, grades, course registrations.
𖦹 Airlines: Reservations and scheduling.
𖦹 Telecommunication: Call/data records, billing, prepaid balance.
𖦹 Web-Based Services: Social media: users, posts, follower networks.
𖦹 Document Databases: Articles, patents, research papers.
𖦹 Navigation Systems: Geographic locations, transport routes.

5. Purpose of Database Systems
𖦹 Universities maintain data about instructors, students, departments, and courses.
𖦹 A file-based system stores data in separate operating-system files with many separate application programs.
𖦹 New departments or rules require new files and new application programs.
𖦹 Over time, the system becomes large, complex, and hard to manage.

6. Problems in File-Processing Systems
𖦹 Data Redundancy and Inconsistency
   • Same data stored in multiple places increases storage and access cost.
   • Different copies of data may disagree (e.g., student address updated in one file but not another).
𖦹 Difficulty in Accessing Data
   • New data requests require new custom programs.
   • Retrieving needed information may require manual filtering or additional programming effort.
𖦹 Data Isolation
   • Data is stored in multiple files with different formats.
   • Hard to write new programs to fetch combined data.
𖦹 Integrity Problems
   • Data must satisfy constraints (e.g., account balance ≥ 0).
   • Constraints are implemented in many programs → hard to update consistently.
𖦹 Atomicity Problems
   • Failures may leave data in inconsistent states.
   • A transaction (e.g., transferring money) must happen entirely or not at all.
𖦹 Concurrent-Access Anomalies
   • Multiple users updating data simultaneously can lead to incorrect results.
   • Example: Two clerks updating the same account or concurrent course registrations causing incorrect counts.
𖦹 Security Problems
   • Not all users should access all data.
   • Hard to enforce security rules when programs are added in an ad-hoc manner.

7. Motivation for Database Systems
𖦹 These problems led to the development of database systems in the 1960s–1970s.
𖦹 Database systems provide solutions for redundancy control, efficient access, integrity enforcement, atomicity, concurrency control, and security.

8. View of Data
𖦹 A database system provides users with an abstract view of data.
𖦹 It hides details of how data is stored and maintained internally.

9. Data Models
𖦹 A data model is a set of concepts used to describe data, relationships, and constraints.
𖦹 Four major categories of data models:
   • Relational Model
     - Uses tables (relations) to represent data and relationships.
     - Each table has rows (records) and columns (attributes).
     - Most widely used model; basis of SQL and modern DBMSs.
   • Entity–Relationship (E-R) Model
     - Uses entities (objects) and relationships among them.
     - Useful for database design and conceptual modeling.
   • Semi-Structured Data Model
     - Allows different data items of the same type to have different attributes.
     - Data formats like JSON and XML fall under this model.
   • Object-Based Data Model
     - Integrates object-oriented programming concepts with databases.
     - Supports objects, methods, encapsulation, and object identity within DB systems.

10. Relational Data Model
𖦹 Data is represented using tables with uniquely named columns.
𖦹 Each row of a table represents one record.
𖦹 Example: instructor table and department table in a university database.

11. Data Abstraction
𖦹 Used to simplify how users interact with stored data.
𖦹 Three levels of data abstraction:
   • Physical Level
     - Describes how data is actually stored (storage structures, indexes, byte layout).
   • Logical Level
     - Describes what data is stored and the relationships among data.
     - Hides physical-level details → provides *physical data independence*.
   • View Level
     - Provides different views to different users.
     - Hides logical-level complexity and acts as a security boundary.

12. Instances and Schemas
𖦹 Instance = The data stored in the database at a specific moment.
𖦹 Schema = The overall structure or design of the database.
𖦹 Types of schemas:
   • Physical Schema → storage-level design.
   • Logical Schema → the logical structure (tables, relationships).
   • View Schemas → user-specific views of data.
𖦹 Application programs should not depend on physical schema → physical data independence.

13. Database Languages
𖦹 A database uses a Data-Definition Language (DDL) to define database schema.
𖦹 It uses a Data-Manipulation Language (DML) to express queries and updates.
𖦹 In practice, DDL and DML are part of a single database language such as SQL.
𖦹 Almost all relational database systems use SQL.

14. Data-Definition Language (DDL)
𖦹 Used to specify the database schema by defining structure and properties of data.
𖦹 Storage structure and access methods are defined using a special form of DDL called data storage and definition language.
   • These details define how data is stored internally and are hidden from end users.
𖦹 DDL allows specification of consistency constraints that must be satisfied by database values.
   • These constraints are checked whenever updates occur.
⠀⠀⠀⠀⠀⠀⠀⠀⠀⣠⢤⠤⢤⡀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⣀⣀⣀⡜⠻⠿⢾⣷⠹⠀⠀⠀⠀⣀⣤⣤⡀⠀⠀⠀⠀
⠀⠀⠀⢠⡎⠁⠀⠀⠃⠀⠀⠀⠉⠀⠀⠀⡠⠞⠛⠛⠻⢽⠀⠀⠀⠀
⠀⠀⠀⠸⣷⡀⠀⢀⠀⢂⠀⡀⠀⠀⠀⠜⠀⠀⠀⠀⡀⠸⠀⠤⢄⠀
⠀⠀⠀⠀⠙⣗⣄⠀⠠⡀⢢⠃⠀⢸⠌⠀⡠⢊⠀⠊⠀⠀⠀⠀⠀⡇
⠀⠀⠀⣀⣀⣈⣲⣕⣦⡈⠢⣻⡀⡎⢀⣴⡢⠑⠂⠁⠀⠀⠀⠀⡠⠃
⠀⣴⠿⠽⠏⠀⠀⠀⠈⠉⢒⣺⣷⣗⣉⣁⣀⣀⣀⠀⠠⠤⠐⠈⠀⠀
⢸⠀⠀⠀⠀⠀⠀⠀⢀⠠⡖⠁⣿⢳⡬⡁⠒⠒⠤⢄⡀⠀⠀⠀⠀⠀
⠈⢦⡀⠀⠀⡀⠄⠂⠁⠊⠀⢰⣯⡏⠎⢌⠑⠀⠀⠀⠈⠑⢄⠀⠀⠀
⠀⠀⠉⡻⠀⠀⠀⠀⠀⠀⣲⡟⡟⠀⠀⠀⠢⡀⠀⠀⠀⠀⢸⠀⠀⠀
⠀⠀⠀⢧⣤⡄⡀⠀⢀⡔⣽⠃⢧⡄⠀⠀⠀⠰⣀⠀⠀⣀⠞⠀⠀⠀
⠀⠀⠀⠈⠳⠬⠥⠔⠋⡜⡌⠀⠘⣿⣦⣀⠀⡀⡇⠉⠉⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⢀⠜⡰⠀⠀⠀⠈⠛⠯⠥⠒⠁⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⡠⠊⡜⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⣛⠊⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀

⭐ DSDL
𖦹 SQL is widely known as DDL (data definition language), DML (data manipulation language), and VDL (view definition language), but it has also been described as SDL (storage definition language).
𖦹 SDL defines how data is physically stored, not the data itself.
𖦹 Examples of SDL include vendor-specific commands like `SET storage_engine=MYISAM;` in MySQL.
𖦹 SDL is not part of the SQL standard; it deals with physical storage, indexing, and data placement.
𖦹 SQL focuses on conceptual and external schemas, while SDL handles internal schemas for storage optimization.
𖦹 Early SQL versions included SDL commands, but these were removed to keep SQL at conceptual and external levels.
𖦹 In practice, DBAs use functions, parameters, and storage specifications to control file storage and indexing.
𖦹 Modern SQL integrates DDL, DML, and VDL into a comprehensive language, but storage definitions remain separate.
𖦹 Three-schema architecture (1970s) inspired the distinction between DDL, VDL, and SDL.
𖦹 Historical references include "Database Sublanguage Faculty" and "View Translation Layer" (1980).
𖦹 Example: CREATE TABLE f (x int, y varchar(25));
   • SDL defines how x and y are stored.
   • DDL defines the table schema: f(x,y) as a relational tuple.
   • VDL defines the view, usually the table name itself.
𖦹 Many databases extend SDL to include indexes and storage backends, which are separate from the conceptual model.
𖦹 Other terms and systems related to SDL: DSDL (Data Storage Definition Language), CODASYL, RDL, NDL, IRDIS.

⭐ Summary of Database Languages
𖦹 DDL defines the structure of the database: tables, columns, constraints, and indexes.
𖦹 VDL defines how data is presented via views without affecting physical storage.
𖦹 SDL defines physical storage details, including tablespaces, filegroups, and storage allocation.
𖦹 Each language serves a distinct purpose but may overlap in commands or functionality in modern DBMSs.
𖦹 DDL and VDL are usually visible to end users through queries, while SDL is managed by DBAs for performance and storage optimization.
𖦹 Examples:
   • DDL: CREATE TABLE Employees (EmpID INT PRIMARY KEY, Name VARCHAR(50), Department VARCHAR(30), Salary DECIMAL(10,2));
   • VDL: CREATE VIEW DeptSalary AS SELECT Department, AVG(Salary) AS AvgSalary FROM Employees GROUP BY Department;
   • SDL (Oracle): CREATE TABLE Employees (...) TABLESPACE Users STORAGE (INITIAL 5M NEXT 5M MAXEXTENTS UNLIMITED);
   • SDL (SQL Server): CREATE TABLE Employees (...) ON [Primary];
   • SDL (PostgreSQL): CREATE TABLE Employees (...) TABLESPACE pg_default;


15. Integrity Constraints in DDL
𖦹 Domain Constraints
   • Each attribute must have an associated domain (e.g., integer, character, date/time).
   • Acts as a basic integrity check performed whenever new data is inserted.
𖦹 Referential Integrity
   • Ensures that values in one relation match values in another.
   • Example: dept_name in course must exist in department relation.
   • If violated, the database system rejects the operation causing the violation.
𖦹 Authorization
   • Controls which users can perform which operations.
   • Common types:
     - Read authorization: can read data but not modify.
     - Insert authorization: can add new data.
     - Update authorization: can modify existing data.
     - Delete authorization: can remove data.
   • A user may have any combination of these permissions.

16. Data Dictionary
𖦹 DDL processing generates output stored in the data dictionary.
𖦹 The data dictionary stores metadata (data about data).
𖦹 Only the database system can directly access or update the data dictionary.
𖦹 The system consults the data dictionary before reading or modifying actual data.

17. SQL Data-Definition Language
𖦹 SQL provides a rich DDL to define tables with data types and integrity constraints.
𖦹 Example table creation:
   create table department
   (dept_name char(20),
    building char(15),
    budget numeric(12,2));
   • This creates a department table with three attributes and associated data types.
𖦹 SQL allows specifying constraints such as:
   • Primary key → ensures uniqueness of dept_name.
   • Referential integrity → ensures dept_name in instructor must appear in department table.

18. Data-Manipulation Language (DML)
𖦹 A DML enables users to access or manipulate data organized by the data model.
𖦹 Types of access provided by DML:
   • Retrieval of information stored in the database.
   • Insertion of new information into the database.
   • Deletion of information from the database.
   • Modification of existing information.
𖦹 Types of DML:
   • Procedural DML:
     - User specifies what data are needed and how to retrieve them.
      -- Step 1: Find employees in 'Sales' department
      OPEN cursor FOR
      SELECT * FROM Employees WHERE Department = 'Sales';
      -- Step 2: Fetch each row and process it
      FETCH NEXT FROM cursor INTO @EmpID, @Name, @Salary;
      WHILE @@FETCH_STATUS = 0
      BEGIN
          PRINT @Name + ' earns ' + CAST(@Salary AS VARCHAR);
          FETCH NEXT FROM cursor INTO @EmpID, @Name, @Salary;
      END
      CLOSE cursor;
   • Declarative (Nonprocedural) DML:
     - User specifies what data are needed without specifying how to retrieve them.
      SELECT Name, Salary
      FROM Employees
      WHERE Department = 'Sales';

𖦹 Declarative DMLs are easier to learn and use compared to procedural DMLs.
𖦹 Since declarative DML does not specify how to get the data, the database system must decide the efficient access method.
𖦹 A query is a statement requesting retrieval of information.
𖦹 The part of DML dealing with data retrieval is called a query language.
𖦹 Commonly, the terms "query language" and "data-manipulation language" are used interchangeably.
𖦹 SQL is the most widely used query language.
𖦹 Levels of abstraction apply to both defining data and manipulating data:
   • Physical level: focuses on efficient data access algorithms.
   • Higher levels: emphasize ease of use for users.
𖦹 The query processor translates DML queries into sequences of physical-level operations.

19. The SQL Data-Manipulation Language
𖦹 SQL is a nonprocedural query language.
𖦹 A query takes one or more tables as input and always returns a single table as output.
𖦹 Example query to retrieve names of instructors in the History department:
   • select instructor.name
     from instructor
     where instructor.dept_name = 'History';
   - Output: A table with a single column (name) and rows for instructors in the History department.
𖦹 Queries may retrieve data from multiple tables.
𖦹 Example query to find IDs and department names of instructors in departments with budget > 95000:
   • select instructor.ID, department.dept_name
     from instructor, department
     where instructor.dept_name = department.dept_name
       and department.budget > 95000;
   - Output: A table with instructor ID and department name for matching records.

20. Database Access from Application Programs
𖦹 SQL alone is not as powerful as a general-purpose programming language.
𖦹 SQL cannot perform:
   • User input operations.
   • Display/output operations.
   • Network communication.
𖦹 For such operations, application programs are written in host languages like C/C++, Java, or Python.
𖦹 These application programs contain embedded SQL queries to interact with the database.
𖦹 Examples of such application programs in a university system:
   • Course registration systems.
   • Class roster generation.
   • GPA calculation systems.
   • Payroll processing systems.
𖦹 To send DML statements from the host program to the database, application program interfaces are used.
𖦹 ODBC standard provides interfaces for C and other languages. (Remember pyodbc for Python)
𖦹 JDBC standard provides interfaces for Java.
𖦹 JDBC standard provides interfaces for Java.
𖦹 An Example using SQLAlchemy as well,
   from sqlalchemy import create_engine
   # Create an engine to a SQLite database
   engine = create_engine('sqlite:///school.db')
   # Using the engine to execute SQL
   with engine.connect() as conn:
       result = conn.execute("SELECT * FROM students")
       for row in result:
           print(row)

21. Database Design
𖦹 Databases manage large bodies of information used in an enterprise.
𖦹 The database may directly produce information or support another product or service.
𖦹 Database design mainly focuses on designing the database schema.
𖦹 A complete database application also needs broader application design beyond schemas.
• Query writing and schema design are primary focus areas initially.
𖦹 A high-level data model helps understand user data needs and structure.
𖦹 The first step is to gather and understand user requirements.
• The designer works with domain experts and users to collect these needs.
• The result is a clear specification of user requirements.
𖦹 The designer chooses a data model and converts requirements into a conceptual schema.
𖦹 The conceptual schema shows a detailed view of the enterprise.
• The designer checks if all data needs are met and removes redundancy.
- This stage focuses on data and relationships, not physical storage.
𖦹 In the relational model, design choices involve selecting attributes and grouping them into tables.
𖦹 Choosing attributes is a business decision, grouping them is a computer science task.
𖦹 Two main approaches:
• Entity-relationship model.
• Normalization algorithms to produce tables.
𖦹 A complete conceptual schema also defines functional requirements.
• Users describe operations like updating, searching, retrieving, and deleting data.
• The designer checks if the schema supports these operations.
𖦹 After conceptual design, two final phases occur:
• Logical design: Map conceptual schema to the system’s data model.
• Physical design: Define file organization and internal storage structures.
- Physical design focuses on how data is stored and accessed efficiently.

22. Database Engine
𖦹 A database system is divided into modules based on responsibilities.
𖦹 Main components are storage manager, query processor, and transaction manager.
𖦹 Storage manager handles large data stored on disks or SSDs.
• Data is moved between disk and main memory when needed.
- Disk access is slower, so data layout should reduce unnecessary data movement.
𖦹 Query processor helps users access data without needing to know physical storage details.
𖦹 Transaction manager ensures a group of operations acts as one complete unit.

23. Storage Manager
𖦹 Acts as the link between low-level stored data and application queries.
𖦹 Interacts with the file manager and translates DML into file-system level actions.
𖦹 Responsible for storing, retrieving, and updating data.
• Authorization and integrity manager checks user permissions and data rules.
• Transaction manager keeps data consistent during failures and concurrency.
• File manager handles disk space and data organization.
• Buffer manager fetches data to main memory and decides what to cache.
𖦹 Uses physical data structures:
• Data files store actual database data.
• Data dictionary stores database structure details.
• Indices provide fast access to specific data items.

24. The Query Processor
𖦹 Translates user queries into efficient execution steps.
• DDL interpreter records schema definitions in the data dictionary.
• DML compiler converts queries into evaluation plans and performs optimization.
• Query evaluation engine executes the chosen low-level instructions.

25. Transaction Management
𖦹 A transaction is a logical unit of work involving multiple database operations.
𖦹 Key requirements:
• Atomicity: All steps happen or none happen.
• Consistency: Data remains correct before and after execution.
• Durability: Results persist even after system failures.
𖦹 Recovery manager restores database state if failures occur.
𖦹 Concurrency-control manager ensures correctness when many transactions run at once.
𖦹 Together they form the transaction manager.

26. Database and Application Architecture
𖦹 Traditional systems used centralized servers with shared memory.
𖦹 Modern systems use parallel and distributed architectures to handle large data.
𖦹 Applications connecting to databases follow:
• Two-tier architecture: Client directly communicates with database server.
• Three-tier architecture: Client → Application server → Database server.
- Three-tier improves security and performance by keeping business logic centralized.

27. Database Users and User Interfaces
𖦹 Database users differ based on how they interact with the system.
𖦹 Naive users use predefined interfaces like web or mobile forms.
• They fill out forms or view reports without knowing database details.
• Example: A student registering for a class through an online form.
𖦹 Application programmers write programs that interact with the database.
𖦹 Sophisticated users use query languages or analysis tools to work directly with data.

28. Database Administrator (DBA)
𖦹 The DBA has central control over data and the programs accessing the data.
𖦹 Responsibilities include:
• Schema definition: Creating the database schema using DDL statements.
• Defining storage structure and access methods: Setting physical data organization and indices.
• Modifying schema and physical layout: Updating structure as needs change or to improve performance.
• Granting authorizations: Controlling who can access which parts of the database.
• Routine maintenance:
- Backing up data to prevent loss.
- Ensuring sufficient disk space.
- Monitoring database performance and managing heavy workloads.

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[08/11/2025]
29. Introduction to the Relational Model
𖦹 The relational model is widely used in commercial data-processing.
𖦹 Its simplicity makes it easier for programmers compared to earlier models.
• Earlier models include network and hierarchical models.
𖦹 The model has evolved with features like object-relational data types, stored procedures, and support for XML and semi-structured data.
𖦹 Its independence from low-level storage allows it to remain relevant despite new storage systems like column stores.

⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡀⣄⡆⡦⢔⢔⢴⡤⡀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⢀⣔⡪⢟⢗⢕⢅⣜⣌⣬⣼⣼⣴⣥⣀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⢙⢻⡶⣕⢵⣫⣿⣷⡿⣿⠟⣿⡿⢿⣿⣿⣶⣄⠀⠀⠀⠀
⢀⣀⡀⠀⠀⠀⠀⠀⠿⣯⢝⣵⡟⣿⠃⡯⠀⡿⢁⣿⢁⣿⣏⠙⠿⠛⣧⡀⠀⠀
⢿⣿⠿⣿⣿⣶⣶⣴⣾⣿⠛⣿⠀⡿⢸⡇⢸⡇⢸⡇⢸⢿⠿⠀⠠⠀⣽⡿⣄⠀
⢀⣽⣭⣽⣿⡏⠟⠉⡇⢸⠀⣿⠀⠙⠭⠽⢛⣳⣽⣧⣿⡏⡆⠀⣇⠀⠉⢁⠝⡃
⢼⣵⠟⠉⠁⠁⠀⣴⣟⠢⠀⠘⠀⠇⢔⣒⣛⠿⡿⠟⠃⠀⠈⠢⡀⠀⠀⢀⠩⠃
⠈⠁⠀⠀⠀⠀⢸⣟⢿⣣⣕⠄⡀⠀⠀⢀⣀⣀⣤⣤⣤⠤⠤⠤⠴⠛⠋⠁⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠻⣿⣿⣵⣥⠦⠏⠉⠉⠀⣠⣾⡟⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠁⠀⠀⠀⠀⠀⠸⡛⠻⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀

⭐ Column Store Storage
𖦹 Data is stored column-by-column instead of row-by-row
• Example layout:
  - ID: 1, 2, 3
  - Name: Rani, Arjun, Kavi
  - Age: 21, 24, 22
  - City: Chennai, Madurai, Trichy

⭐ Row Store Storage
𖦹 Data is stored row-by-row
• Example layout:
  - 1, Rani, 21, Chennai
  - 2, Arjun, 24, Madurai
  - 3, Kavi, 22, Trichy

⭐ Why Column Store
𖦹 Faster analytical queries
• Only required columns are read (e.g., AVG(Age) reads only Age column)
𖦹 Better compression
• Similar data within a column compresses efficiently
𖦹 Suitable for large data warehouses
• Used for OLAP, analytics, and business intelligence

⭐ When Column Store Is Not Good
𖦹 Frequent row updates or inserts are slow
- Reconstructing full rows becomes costly
𖦹 Not ideal for OLTP transactional systems

⭐ Examples of Column-Store Databases
• Amazon Redshift
• Google BigQuery
• Snowflake
• ClickHouse
• Apache Cassandra (wide-column model)
• SAP HANA
• PostgreSQL via cstore_fdw extension

30. Structure of Relational Databases
𖦹 A relational database is a collection of tables, each with a unique name.
𖦹 Each table contains rows (tuples) and columns (attributes).
• Example tables: instructor, course, prereq.
• Each row represents a relationship among attribute values.
𖦹 The term relation refers to a table, tuple refers to a row, and attribute refers to a column.
𖦹 A relation instance is the specific set of rows present at a given time.
𖦹 The order of rows does not matter because a relation is a set of tuples.

31. Attributes and Domains
𖦹 Each attribute has a domain, which is the set of allowed values.
𖦹 Domains must be atomic (indivisible units).
• Example: If phone number stores multiple numbers in one field, it is non-atomic.
• Atomicity depends on how values are used, not just how they appear.
𖦹 Null represents unknown or non-existing values.
• Nulls can cause complications and should be minimized.

32. Suitability and Limitations
𖦹 The strict structure of relational databases allows efficient storage and processing.
𖦹 This structure suits stable and well-defined applications.
𖦹 It is less suitable for situations where data types and structures change frequently.
• Modern systems try to balance efficient structured storage with flexibility.

33. Database Schema
𖦹 Database schema is the logical design of the database.
𖦹 Database instance is the data in the database at a specific moment.
𖦹 Relation schema lists attributes and their domains.
𖦹 Relation instance is the current set of tuples and can change over time while the schema usually does not.
𖦹 Same name may refer to either schema or instance; context makes the meaning clear.
𖦹 Common attributes in different relation schemas help link data between relations.

34. Example Schemas in a University Database
𖦹 department(dept name, building, budget)
𖦹 section(courseid, secid, semester, year, building, roomnumber, timeslot id)
𖦹 teaches(ID, courseid, secid, semester, year)
𖦹 Additional relations: student, advisor, takes, classroom, timeslot.

35. Keys
𖦹 Tuples in a relation must be uniquely identifiable.
𖦹 Superkey: A set of attributes that uniquely identifies a tuple.
• Example: instructor.ID is a superkey.
𖦹 Candidate key: A minimal superkey with no unnecessary attributes.
𖦹 Primary key: A chosen candidate key used as the main tuple identifier.
• Primary keys should use attributes that rarely change.
𖦹 Primary key attributes are listed first in the schema and are underlined.
• Example: classroom(building, roomnumber, capacity) uses building + roomnumber as primary key.

36. Foreign Keys and Referential Integrity
𖦹 A foreign key in one relation refers to the primary key of another relation.
• Example: instructor.dept name references department.dept name.
𖦹 The referencing relation is the one containing the foreign key; the referenced relation contains the primary key.
𖦹 Foreign keys ensure related data remains valid across relations.
𖦹 Referential integrity ensures values in the referencing relation also exist in the referenced relation, even if the referenced attribute is not a primary key.

37. Schema Diagrams
𖦹 Schema diagrams visually represent a database schema along with its primary and foreign key constraints.
𖦹 Each relation is shown as a box containing its name and attributes.
• Primary key attributes are underlined.
• Foreign key relationships are shown using arrows from referencing attributes to referenced primary keys.
𖦹 A two-headed arrow indicates a referential integrity constraint that is not a foreign key constraint.
𖦹 Example: In the diagram, section.time slot id links to time slot.time slot id with a two-headed arrow to show referential integrity.
𖦹 Many database systems provide graphical tools to create such diagrams.
𖦹 Schema diagrams should not be confused with entity-relationship diagrams.
IMG_URL schema-diagram.png

38. Relational Query Languages
𖦹 A query language allows users to request information from a database.
𖦹 Query languages are usually higher-level than standard programming languages.
𖦹 Types of query languages:
• Imperative query languages
- User specifies a sequence of operations and state changes to get the desired result.
• Functional query languages
- Computation is expressed as evaluation of functions without side effects.
• Declarative query languages
- User specifies what information is required, not how to obtain it. The system figures out the steps.
𖦹 Examples of “pure” query languages:
• Relational algebra (functional) – theoretical basis of SQL.
• Tuple relational calculus and domain relational calculus (declarative).
𖦹 Practical query languages like SQL combine imperative, functional, and declarative styles.

39. The Relational Algebra
𖦹 Relational algebra uses operations on relations to produce new relations.
𖦹 Unary operations work on one relation (select, project, rename).
𖦹 Binary operations work on two relations (union, Cartesian product, set difference).
𖦹 Relational algebra is the theoretical basis for SQL, but users do not write queries directly in it.
𖦹 Relations are sets, so duplicates are not allowed in formal relational algebra.

40. The Select Operation (σ)
𖦹 Selects tuples that satisfy a given condition.
𖦹 Written as: σ(condition)(relation)
𖦹 Conditions may use =, ≠, <, ≤, >, ≥ and logical connectives (∧, ∨, ¬).
• Example: σdeptname="Physics"(instructor)
- Returns only instructors in the Physics department.

41. The Project Operation (Π)
𖦹 Selects specific attributes (columns) from a relation.
𖦹 Written as: Πattribute_list(relation)
𖦹 Duplicate rows are removed.
• Example: ΠID,name,salary(instructor)
- Produces a relation with only these columns.

42. Composition of Relational Operations
𖦹 The result of one relational operation can be used as input to another.
𖦹 Allows nested expressions.
• Example: Πname(σdeptname="Physics"(instructor))

43. The Cartesian Product (×)
𖦹 Combines tuples from two relations into all possible pairs.
𖦹 Written as: r1 × r2
𖦹 Produces a large relation with concatenated attributes.
• If both relations share attribute names, relation names are prefixed to distinguish them.

44. The Join Operation (⋈)
𖦹 Combines related tuples from two relations based on a condition.
𖦹 Defined as: r ⋈θ s = σθ(r × s)
• Example: instructor ⋈ instructor.ID = teaches.ID teaches
- Combines instructor and teaches only when IDs match.
𖦹 More useful than Cartesian product because it avoids unrelated pairings.

45. Set Operations (∪, ∩, −)
𖦹 Work only on compatible relations (same number and type of attributes).
𖦹 Union (r ∪ s): Tuples in either relation.
𖦹 Intersection (r ∩ s): Tuples common to both relations.
𖦹 Difference (r − s): Tuples in r but not in s.
• Example: Πcourseid(σsemester="Fall"∧year=2017(section)) ∪ Πcourseid(σsemester="Spring"∧year=2018(section))

46. The Assignment Operation (←)
𖦹 Stores the result of an expression in a temporary relation variable.
𖦹 Used to simplify complex queries.
• Example:
courses_fall ← Πcourseid(...)
courses_spring ← Πcourseid(...)
courses_fall ∩ courses_spring

47. The Rename Operation (ρ)
𖦹 Gives a name to the result of an expression or renames attributes.
𖦹 Written as: ρnew_name(expression) or ρnew_name(A1,A2,...,An)(expression)
• Used when the same relation must be referenced more than once in a query.

48. Equivalent Queries
𖦹 Different relational algebra expressions can produce the same result.
𖦹 Example:
σdeptname="Physics"(instructor ⋈ instructor.ID=teaches.ID teaches)
(σdeptname="Physics"(instructor)) ⋈ instructor.ID=teaches.ID teaches
𖦹 Query optimizers choose efficient equivalent expressions during execution.

49. Introduction to SQL
𖦹 SQL is a widely used database language.
𖦹 It can define, modify, and secure data in a database, not just query it.
𖦹 The focus is on SQL’s main ideas, not a full user guide.
𖦹 SQL features may vary between systems.
𖦹 Users are encouraged to try example SQL queries on real databases.

50. Overview of the SQL Query Language
𖦹 SQL was first developed by IBM in the 1970s under the name SEQUEL.
𖦹 It became SQL (Structured Query Language) and is now the standard for relational databases.
𖦹 ANSI and ISO established SQL standards with updates over time (SQL-86 to SQL:2016).
𖦹 Main components of SQL:
• Data-definition language (DDL) – defines and modifies table structures.
• Data-manipulation language (DML) – retrieves and updates data.
• Integrity – defines rules to ensure data accuracy.
• View definition – defines virtual tables (views).
• Transaction control – defines transaction start and end points.
• Embedded and dynamic SQL – allows SQL use in programming languages.
• Authorization – controls user access rights.
𖦹 SQL implementations differ slightly, with some supporting only certain features.

51. SQL Data Definition
𖦹 The data-definition language (DDL) specifies database structures.
𖦹 It defines details such as:
• Table schemas
• Data types for each attribute
• Integrity constraints
• Indexes to maintain
• Security and authorization settings
• Physical storage structure
𖦹 The focus here is on basic types and table definitions.

52. Basic Types in SQL
𖦹 SQL supports several built-in data types:
• char(n): Fixed-length string.
• varchar(n): Variable-length string.
• int or integer: Whole numbers.
• smallint: Smaller integer type.
• numeric(p,d): Fixed-point number with defined precision.
• real, double precision: Floating-point types.
• float(n): Floating-point with at least n digits of precision.
𖦹 Each type may allow a null value, representing unknown or missing data.
𖦹 char stores fixed-length strings by adding spaces; varchar stores only the actual string.
𖦹 Comparing char and varchar can behave differently across systems.
𖦹 varchar is preferred to avoid comparison issues.
𖦹 nvarchar supports multilingual data using Unicode; some databases also allow Unicode in varchar.

53. Basic Schema Definition
𖦹 Tables are created using the create table command.
• Example:
  create table department
  (dept_name varchar(20),
   building varchar(15),
   budget numeric(12,2),
   primary key (dept_name));
𖦹 Each attribute has a name, data type, and optional constraints.
𖦹 Common integrity constraints:
• primary key (A1, A2, ...): Ensures uniqueness and non-null values.
• foreign key (A1, A2, ...) references table: Ensures values match existing records in another table.
• not null: Prevents null values for that attribute.
𖦹 SQL rejects updates that break integrity constraints.
𖦹 Tables are empty when created; data is added using insert, update, and delete commands.
𖦹 drop table r; removes a table and its schema.
𖦹 delete from r; removes all data but keeps the table structure.
𖦹 alter table allows structure changes:
• alter table r add A D; adds a new attribute with null values.
• alter table r drop A; removes an attribute.
𖦹 Some systems do not allow attribute deletion but allow table deletion.

54. Basic Structure of SQL Queries
𖦹 An SQL query has three main clauses: select, from, and where.
𖦹 The from clause lists input tables (relations).
𖦹 The where clause filters rows based on a condition.
𖦹 The select clause specifies which columns or expressions to display.
𖦹 A query takes input relations, processes them, and returns a new relation as output.

55. Queries on a Single Relation
𖦹 Example: To find all instructor names:
• select name from instructor;
- Returns one column, "name", from the instructor table.
𖦹 Example: To find department names of all instructors:
• select dept_name from instructor;
- Duplicate department names may appear if multiple instructors belong to the same department.
𖦹 SQL allows duplicate tuples in tables and query results for efficiency.
𖦹 To remove duplicates, use distinct:
• select distinct dept_name from instructor;
𖦹 To explicitly keep duplicates, use all (though it is the default):
• select all dept_name from instructor;
𖦹 The select clause can also contain arithmetic operations:
• select ID, name, dept_name, salary * 1.1 from instructor;
- This multiplies salary by 1.1 but does not modify stored data.
𖦹 The where clause filters rows that meet certain conditions.
𖦹 Example: Find names of instructors in the Computer Science department with salary > 70000:
• select name from instructor where dept_name = 'Comp. Sci.' and salary > 70000;
𖦹 Logical connectives (and, or, not) and comparison operators (<, <=, >, >=, =, <>) can be used in conditions.
𖦹 Conditions can compare numbers, strings, or special types like dates.

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[09/11/2025]
56. Queries on Multiple Relations
𖦹 Queries can combine data from multiple tables.
𖦹 Example: Retrieve instructor names with department names and building names:
• select name, instructor.dept_name, building
  from instructor, department
  where instructor.dept_name = department.dept_name;
- Matches each instructor with the corresponding department using dept_name.
𖦹 When an attribute name appears in multiple tables, prefix it with the table name.
𖦹 Example: instructor.dept_name distinguishes it from department.dept_name.
𖦹 Each table listed in the from clause must have a unique name.
𖦹 Typical SQL query format:
• select A1, A2, …, An
  from r1, r2, …, rm
  where P;
- Ai: attributes to output
- ri: tables used
- P: condition to filter results
𖦹 If the where clause is omitted, all possible combinations (Cartesian product) are returned.

57. Operational Understanding of SQL Queries
𖦹 Logical order of execution:
• 1. from – create a Cartesian product of all tables.
• 2. where – filter rows using conditions.
• 3. select – display the chosen columns or expressions.
𖦹 The from clause forms combinations of all tuples from listed tables.
𖦹 Example schema for instructor × teaches:
(instructor.ID, instructor.name, instructor.dept_name, instructor.salary,
 teaches.ID, teaches.course_id, teaches.sec_id, teaches.semester, teaches.year)
𖦹 Cartesian product pairs each row from one table with every row in the other.
𖦹 It can produce huge results and usually includes unrelated tuples.
𖦹 To get meaningful combinations, add a condition in the where clause to match related tuples.
𖦹 Example: Match instructors with the courses they teach:
• select name, course_id
  from instructor, teaches
  where instructor.ID = teaches.ID;
- Returns only instructors who have taught a course.
𖦹 Example: Restrict to Computer Science department:
• select name, course_id
  from instructor, teaches
  where instructor.ID = teaches.ID and dept_name = 'Comp. Sci.';
𖦹 General meaning of an SQL query:
• Step 1: Create Cartesian product of tables in from clause.
• Step 2: Apply filtering condition from where clause.
• Step 3: Output attributes listed in select clause.
𖦹 Omitting the where clause causes a full Cartesian product, which can be very large and inefficient.

58. Additional Basic Operations
𖦹 SQL supports several additional operations to enhance query flexibility and output formatting.

59. The Rename Operation
𖦹 The as clause allows renaming of attributes or tables in SQL.
• Syntax: old-name as new-name
• Can be used in both select and from clauses.
𖦹 Example of renaming an attribute:
• select name as instructor_name, course_id from instructor, teaches where instructor.ID = teaches.ID;
𖦹 Example of renaming relations for convenience:
• select T.name, S.course_id from instructor as T, teaches as S where T.ID = S.ID;
- T and S act as aliases for instructor and teaches.
𖦹 Renaming is necessary when:
• Two tables have attributes with the same name.
• Expressions are used in select and need a name.
• Shorter or more meaningful names are desired.
𖦹 Renaming the same relation helps compare tuples within it.
• Example: Find instructors whose salary is greater than any instructor in the Biology department:
  select distinct T.name
  from instructor as T, instructor as S
  where T.salary > S.salary and S.dept_name = 'Biology';
𖦹 The alias names (like T and S) are also called correlation names or table aliases.
𖦹 Some SQL systems (like Oracle) omit the keyword as in the from clause.

60. String Operations
𖦹 String literals are enclosed in single quotes ('example').
• To include a single quote inside, use two single quotes: 'It''s right'.
𖦹 String comparison using '=' is case-sensitive in standard SQL.
• 'comp. sci.' = 'Comp. Sci.' returns false in standard SQL.
• Some systems like MySQL treat strings as case-insensitive by default.
𖦹 Common string functions:
• Concatenation using || 
• upper(s), lower(s), trim(s)
• substring extraction and length calculation
𖦹 Pattern matching uses the like operator.
• % matches any substring.
• _ matches any single character.
• Examples:
  - 'Intro%' matches strings beginning with 'Intro'.
  - '%Comp%' matches any string containing 'Comp'.
  - '___' matches any three-character string.
𖦹 Example query:
• select dept_name from department where building like '%Watson%';
𖦹 To include % or _ as literal characters, use escape:
• like 'ab\%cd%' escape '\'
- Matches strings beginning with “ab%cd”.
𖦹 To find mismatches, use not like.
𖦹 Some systems like PostgreSQL offer similar to for advanced pattern matching (similar to regular expressions).

61. Attribute Specification in the Select Clause
𖦹 The asterisk (*) selects all attributes.
• Example:
  select instructor.* from instructor, teaches where instructor.ID = teaches.ID;
- Selects all columns from instructor.
𖦹 select * selects all attributes from all relations in the from clause.

62. Ordering the Display of Tuples
𖦹 The order by clause controls the sort order of query results.
• Example: List instructors in the Physics department alphabetically:
  select name from instructor where dept_name = 'Physics' order by name;
𖦹 Default order is ascending (asc).
• Use desc for descending order.
𖦹 Multiple attributes can be used for sorting.
• Example: Sort by salary (descending), then by name (ascending):
  select * from instructor order by salary desc, name asc;

63. Where-Clause Predicates
𖦹 The between operator simplifies range checks.
• Example:
  select name from instructor where salary between 90000 and 100000;
- Equivalent to: salary <= 100000 and salary >= 90000.
𖦹 Use not between for the opposite condition.
𖦹 SQL allows tuple comparisons using row constructors (v1, v2, ... , vn).
• Example:
  select name, course_id
  from instructor, teaches
  where (instructor.ID, dept_name) = (teaches.ID, 'Biology');
- Compares multiple attributes as one tuple.
𖦹 Tuple comparisons are evaluated lexicographically.

64. Set Operations
𖦹 SQL supports union, intersect, and except operations, similar to set operations in mathematics.
𖦹 Example tables:
• c1: courses taught in Fall 2017.
• c2: courses taught in Spring 2018.

65. The Union Operation
𖦹 Combines results of two queries, removing duplicates by default.
• Example:
  (select course_id from section where semester = 'Fall' and year = 2017)
  union
  (select course_id from section where semester = 'Spring' and year = 2018);
𖦹 Use union all to retain duplicates.
- The number of duplicates equals total occurrences in both results.

66. The Intersect Operation
𖦹 Returns tuples common to both query results, removing duplicates by default.
• Example:
  (select course_id from section where semester = 'Fall' and year = 2017)
  intersect
  (select course_id from section where semester = 'Spring' and year = 2018);
𖦹 Use intersect all to retain duplicates.
- Number of duplicates equals the minimum number of occurrences in both sets.
𖦹 Some systems like MySQL do not support intersect directly.

67. The Except Operation
𖦹 Returns tuples from the first query not found in the second.
• Example:
  (select course_id from section where semester = 'Fall' and year = 2017)
  except
  (select course_id from section where semester = 'Spring' and year = 2018);
𖦹 Automatically removes duplicates before performing set difference.
𖦹 Use except all to keep duplicates.
- Result count = copies in first - copies in second (if positive).
𖦹 Some systems (like Oracle) use minus instead of except.
𖦹 MySQL does not implement except; subqueries can be used as alternatives.

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[10/11/2025]
68. Null Values
𖦹 Null values create special issues in arithmetic, comparison, and set operations.
𖦹 Any arithmetic expression with a null input results in null.
• Example: if r.A is null, then r.A + 5 is also null.
𖦹 Comparisons with null values do not result in true or false but in unknown.
- Example: “1 < null” is neither true nor false, so its result is unknown.
𖦹 SQL introduces a third logical value: unknown.
𖦹 Boolean operations are extended to handle unknown.
• and: true and unknown → unknown; false and unknown → false; unknown and unknown → unknown.
• or: true or unknown → true; false or unknown → unknown; unknown or unknown → unknown.
• not: not unknown → unknown.
𖦹 If a where condition evaluates to false or unknown, the tuple is not included in the result.
𖦹 Null checks use special predicates:
• is null → true if the value is null.
• is not null → true if the value is not null.
𖦹 Example:
  select name
  from instructor
  where salary is null;
𖦹 The predicate is unknown can test if a comparison result is unknown.
• Example:
  select name
  from instructor
  where salary > 10000 is unknown;
𖦹 When using select distinct, tuples are considered identical if both have nulls in the same attributes.
• Example: ('A', null) and ('A', null) are treated as identical.
𖦹 For distinct, union, intersection, and except, nulls in the same position are treated as equal.
𖦹 In normal comparisons, null = null is unknown, but in duplicate elimination it is treated as equal.
𖦹 Some databases do not support is unknown and is not unknown.

69. Aggregate Functions
𖦹 Aggregate functions take a collection of values and return a single value.
𖦹 Standard aggregate functions:
• avg – average value
• min – minimum value
• max – maximum value
• sum – total
• count – number of items
𖦹 sum and avg require numeric inputs; others can work on non-numeric data too.

70. Basic Aggregation
𖦹 Aggregates can summarize information from relations.
• Example: Find the average salary in the Computer Science department.
  select avg(salary)
  from instructor
  where dept_name = 'Comp. Sci.';
𖦹 The result is one value (e.g., avg salary = 77333.33).
𖦹 We can rename the output attribute using as:
  select avg(salary) as avg_salary
  from instructor
  where dept_name = 'Comp. Sci.';
𖦹 Duplicate tuples affect aggregate results.
- Duplicates are normally included in averages unless distinct is specified.
𖦹 To eliminate duplicates in aggregation, use distinct.
• Example: Count unique instructors teaching in Spring 2018:
  select count(distinct ID)
  from teaches
  where semester = 'Spring' and year = 2018;
𖦹 count(*) counts all tuples in a relation.
• Example:
  select count(*)
  from course;
𖦹 distinct cannot be used with count(*).
𖦹 all keyword keeps duplicates but is optional since it is the default.

71. Aggregation with Grouping
𖦹 group by divides tuples into groups based on attribute values.
𖦹 Each group is processed separately for aggregation.
• Example: Find average salary per department.
  select dept_name, avg(salary) as avg_salary
  from instructor
  group by dept_name;
𖦹 Without group by, the whole table is treated as one group.
𖦹 Only attributes in the group by clause can appear in select without being aggregated.
𖦹 Example of valid query:
  select dept_name, count(distinct ID) as instr_count
  from instructor, teaches
  where instructor.ID = teaches.ID and semester = 'Spring' and year = 2018
  group by dept_name;
𖦹 Example of invalid query:
  select dept_name, ID, avg(salary)
  from instructor
  group by dept_name;
- Error: ID not in group by and not aggregated.
𖦹 Comments in SQL can be written as /* comment */ or -- comment.

72. The Having Clause
𖦹 having filters groups after aggregation, similar to where for individual tuples.
𖦹 Example: Find departments where average salary > 42000.
  select dept_name, avg(salary) as avg_salary
  from instructor
  group by dept_name
  having avg(salary) > 42000;
𖦹 Any non-aggregated attribute in having must also appear in group by.
𖦹 SQL query evaluation order with grouping:
• 1. Evaluate from clause to get relation.
• 2. Apply where condition if present.
• 3. Group tuples using group by (if absent, all tuples form one group).
• 4. Apply having clause to remove groups not meeting the condition.
• 5. Use select to compute and display results.
𖦹 Example with both where and having:
  select course_id, semester, year, sec_id, avg(tot_cred)
  from student, takes
  where student.ID = takes.ID and year = 2017
  group by course_id, semester, year, sec_id
  having count(ID) >= 2;

73. Aggregation with Null and Boolean Values
𖦹 Null values affect aggregate results.
𖦹 sum, avg, min, and max ignore null values; count(*) counts all rows.
• Example: select sum(salary) from instructor;
  - Null salaries are ignored in the total.
𖦹 If all values are null, count(*) = 0 and others (sum, avg, etc.) return null.
𖦹 SQL:1999 introduced Boolean type (true, false, unknown).
𖦹 Aggregate functions on Boolean values:
• some – returns true if at least one value is true (logical OR)
• every – returns true only if all values are true (logical AND)

74. Nested Subqueries
𖦹 SQL allows queries to be nested within other queries.
𖦹 A subquery is a select-from-where statement inside another query.
𖦹 Common uses include checking set membership, comparing sets, and calculating set size.
• Subqueries can appear in where, from, or select clauses.
- Subqueries returning one value are called scalar subqueries.

75. Set Membership
𖦹 The in operator checks if a value is present in a set.
𖦹 The not in operator checks if a value is not in a set.
• Example using in:
  select distinct course_id from section
  where semester='Fall' and year=2017 and
  course_id in (select course_id from section where semester='Spring' and year=2018);
- Finds courses taught in both Fall 2017 and Spring 2018.
• Example using not in:
  select distinct course_id from section
  where semester='Fall' and year=2017 and
  course_id not in (select course_id from section where semester='Spring' and year=2018);
- Finds courses taught in Fall 2017 but not in Spring 2018.
𖦹 The in and not in operators can also work with fixed lists of values.
• Example:
  select name from instructor where name not in ('Mozart','Einstein');
𖦹 Membership tests can include multiple attributes.
• Example:
  select count(distinct ID)
  from takes
  where (course_id, sec_id, semester, year) in
  (select course_id, sec_id, semester, year from teaches where teaches.ID='10101');
- Counts students who took any section taught by a specific instructor.

76. Set Comparison
𖦹 Subqueries can be compared to sets using some or all.
• > some means “greater than at least one value.”
• > all means “greater than all values.”
• Example using some:
  select name from instructor
  where salary > some (select salary from instructor where dept_name='Biology');
- Finds instructors earning more than at least one Biology instructor.
• Example using all:
  select name from instructor
  where salary > all (select salary from instructor where dept_name='Biology');
- Finds instructors earning more than every Biology instructor.
𖦹 The keyword any is a synonym for some.
𖦹 Example with group comparison:
  select dept_name
  from instructor
  group by dept_name
  having avg(salary) >= all (select avg(salary)
                             from instructor
                             group by dept_name);
- Finds departments with the highest average salary.

77. Test for Empty Relations
𖦹 The exists operator checks if a subquery returns any rows.
• Example:
  select course_id
  from section as S
  where semester='Fall' and year=2017 and
  exists (select * from section as T
          where semester='Spring' and year=2018 and S.course_id=T.course_id);
- Finds courses offered in both semesters.
𖦹 Subqueries using values from outer queries are called correlated subqueries.
𖦹 The not exists operator checks if a subquery returns no rows.
• Example:
  select S.ID, S.name
  from student as S
  where not exists ((select course_id from course where dept_name='Biology')
                    except
                    (select T.course_id from takes as T where S.ID=T.ID));
- Finds students who have taken all Biology courses.
𖦹 exists can replace tuple constructors not supported by all databases.

78. Test for the Absence of Duplicate Tuples
𖦹 The unique operator checks if a subquery result has no duplicate tuples.
• Example:
  select T.course_id
  from course as T
  where unique (select R.course_id
                from section as R
                where T.course_id=R.course_id and R.year=2017);
- Finds courses offered at most once in 2017.
𖦹 The not unique operator checks for duplicate tuples.
• Example:
  select T.course_id
  from course as T
  where not unique (select R.course_id
                    from section as R
                    where T.course_id=R.course_id and R.year=2017);
- Finds courses offered at least twice in 2017.
𖦹 If any attribute is null, unique may return true even when duplicates exist.

79. Subqueries in the From Clause
𖦹 A subquery can be treated as a temporary relation in the from clause.
• Example:
  select dept_name, avg_salary
  from (select dept_name, avg(salary) as avg_salary
        from instructor
        group by dept_name)
  where avg_salary > 42000;
- Finds departments with average salary above 42,000.
𖦹 Subqueries in from can be named and their columns renamed using as.
• Example:
  select dept_name, avg_salary
  from (select dept_name, avg(salary)
        from instructor
        group by dept_name)
  as dept_avg(dept_name, avg_salary)
  where avg_salary > 42000;
𖦹 Most SQL systems require a name for subqueries in from.
𖦹 Example for maximum total salary per department:
  select max(tot_salary)
  from (select dept_name, sum(salary)
        from instructor
        group by dept_name)
  as dept_total(dept_name, tot_salary);
𖦹 The lateral keyword allows a subquery to access outer variables in the same from clause.
• Example:
  select name, salary, avg_salary
  from instructor I1,
       lateral (select avg(salary) as avg_salary
                from instructor I2
                where I2.dept_name = I1.dept_name);

80. The With Clause
𖦹 Defines temporary relations used within a query.
• Example:
  with max_budget(value) as
  (select max(budget) from department)
  select budget
  from department, max_budget
  where department.budget = max_budget.value;
- Finds departments with the maximum budget.
𖦹 Makes complex queries easier to read and reuse.
• Example:
  with dept_total(dept_name, value) as
  (select dept_name, sum(salary)
   from instructor
   group by dept_name),
  dept_total_avg(value) as
  (select avg(value)
   from dept_total)
  select dept_name
  from dept_total, dept_total_avg
  where dept_total.value > dept_total_avg.value;
- Finds departments where total salary is above the average total.

81. Scalar Subqueries
𖦹 A scalar subquery returns a single value.
𖦹 Can appear in select, where, or having clauses.
• Example:
  select dept_name,
         (select count(*)
          from instructor
          where department.dept_name = instructor.dept_name)
         as num_instructors
  from department;
- Lists each department and its instructor count.
𖦹 If a scalar subquery returns more than one value, it causes an error at runtime.

82. Scalar Without a From Clause
𖦹 Some queries compute values without referencing tables.
• Example:
  (select count(*) from teaches)/(select count(*) from instructor);
- Finds average number of sections per instructor.
𖦹 Some systems require a dummy table like dual.
• Example:
  select (select count(*) from teaches)/(select count(*) from instructor)
  from dual;
𖦹 Multiply by 1.0 if a floating-point result is needed.

83. SQL and Multiset Relational Algebra (Part 3)
𖦹 Subqueries have no direct equivalent in relational algebra.
𖦹 Many subqueries can be rewritten using semijoin (⋉) and antijoin (⋉).
• r ⋉ s returns tuples in r with matching tuples in s.
• r ⋉ s returns tuples in r with no matching tuples in s.
𖦹 These are efficient and often used internally by databases.
𖦹 Rewriting subqueries is complex, so modern systems extend relational algebra to allow subqueries in predicates and projections.

84. Deletion in SQL
𖦹 SQL can delete whole tuples from a table, not individual attribute values.
𖦹 Syntax: delete from r where P;
• r is the relation, P is the predicate.
- If where clause is omitted, all tuples in the relation are deleted.
𖦹 Delete operates on one relation at a time; multiple relations require separate delete commands.
𖦹 The predicate in the where clause can be complex, including nested selects.
𖦹 Examples:
• Delete all instructors in the Finance department: delete from instructor where dept_name = 'Finance';
• Delete instructors with salary between $13,000 and $15,000.
• Delete instructors associated with departments in the Watson building using a nested select.
𖦹 Nested selects can reference the same relation being deleted.
• Example: delete instructors with salary below the average salary.

85. Insertion in SQL
𖦹 Insert data by specifying a tuple or using a query result.
𖦹 Attribute values must match the domain and number of attributes in the relation.
𖦹 Basic syntax: insert into course values ('CS-437', 'Database Systems', 'Comp. Sci.', 4);
𖦹 Attributes can be specified explicitly in the insert statement.
𖦹 Insert using query results:
• Example: make students in Music department with >144 credits instructors with $18,000 salary.
𖦹 SQL evaluates the select fully before insertion to avoid infinite loops or duplicates.
𖦹 Partial attribute insertion assigns null to unspecified attributes.
• Example: insert into student values ('3003', 'Green', 'Finance', null);
𖦹 Bulk loader utilities allow fast insertion of many tuples from formatted files.

86. Updates in SQL
𖦹 Update changes values in existing tuples without changing all attributes.
𖦹 Syntax: update relation set attribute = value where condition;
𖦹 Example: increase all instructor salaries by 5%.
𖦹 Conditional updates using where clause:
• Example: increase salary by 5% only for instructors earning less than $70,000.
𖦹 Nested selects can be used in update statements, referencing the same relation.
• Example: increase salary by 5% for those below the average salary.
𖦹 Multiple conditional updates may require careful ordering.
• Example: 3% raise for salaries >$100,000, 5% for others.
𖦹 Case statements allow combining multiple conditions in one update.
• Syntax: 
  case
    when pred1 then result1
    when pred2 then result2
    ...
    else result0
  end
𖦹 Scalar subqueries can be used in set clause.
• Example: set student tot_cred = sum of credits from successfully completed courses.
𖦹 Handle nulls using case expression or coalesce function.
• Example: tot_cred = coalesce(sum(credits), 0)

87. Intermediate SQL
𖦹 Study of more complex SQL queries
𖦹 View definitions, transactions, integrity constraints
𖦹 SQL data definition and authorization details

88. Join Expressions
𖦹 Combines information from multiple relations
𖦹 Provides a more natural way to write queries than Cartesian product
𖦹 Can express queries difficult with only Cartesian product

89. The Natural Join
𖦹 Matches tuples with same values in common attributes
𖦹 Example: student natural join takes matches on ID
𖦹 Result includes only students who have taken courses
𖦹 Attributes common to both relations appear once
𖦹 Query equivalence: 
• select name, courseid from student, takes where student.ID = takes.ID
• select name, courseid from student natural join takes
- Both give same results
𖦹 Multiple natural joins can be combined:
• select A1, A2,…, An from r1 natural join r2 natural join ... natural join rm where P
𖦹 Using join with specific columns:
• Example: select name, title from (student natural join takes) join course using (courseid)
- Ensures only specified attributes are matched

90. Join Conditions
𖦹 General join using arbitrary conditions with ON clause
𖦹 Example: select * from student join takes on student.ID = takes.ID
𖦹 ON condition can replicate WHERE clause behavior
𖦹 Improves readability and handles outer joins differently
𖦹 ON condition supports any SQL predicate for flexible joins

91. Outer Joins
𖦹 Preserves tuples that would be lost in inner joins
𖦹 Types of outer joins:
• Left outer join: preserves tuples from left relation
• Right outer join: preserves tuples from right relation
• Full outer join: preserves tuples from both relations
𖦹 Preserves unmatched tuples by filling non-matching attributes with nulls
𖦹 Example: student natural left outer join takes includes students with no courses
𖦹 Query to find students who have not taken any course:
• select ID from student natural left outer join takes where courseid is null
𖦹 Right outer join works symmetrically, order of relations affects result attribute order

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[11/11/2025]
92. Purpose of Views  
𖦹 Not all users should see full data in all relations  
𖦹 Views allow showing only selected attributes  
• Example: Clerk can see ID, name, dept_name but not salary  

93. Personalized Virtual Relations  
𖦹 Views help present data in a more intuitive structure for specific users  
• Example: List Physics department course sections in Fall 2017 with building and room info  

94. Virtual Relation Behavior  
𖦹 Results of a view are not stored permanently  
𖦹 Data is computed whenever the view is used  
- Prevents outdated or inconsistent data  

95. View Definition  
𖦹 Defined using <pink>create view</pink> command  
• Syntax: <pink>create view</pink> <yellow>v as <query expression></yellow>;  
• Example: <pink>create view</pink> <yellow>faculty as</yellow> <pink>select</pink> <yellow>ID, name, dept_name</yellow> <pink>from</pink> <yellow>instructor</yellow>;  

96. Example of Course Section View  
𖦹 Can define view for Physics Fall 2017 sections  
• View stores query definition, not results  
• Recomputed when accessed  

97. Persistence of Views  
𖦹 Views remain available until explicitly dropped  
𖦹 Unlike <pink>with</pink> clause which is local to a single query  

98. Using Views in Queries  
𖦹 View name used like a table name  
• Example: <pink>select</pink> <yellow>course_id</yellow> <pink>from</pink> <yellow>physics_fall_2017</yellow> <pink>where</pink> <yellow>building = 'Watson'</yellow>;  
𖦹 Attribute names can be explicitly set in view definition  

99. Views Recomputed Automatically  
𖦹 View content is conceptually the result of the defining query  
𖦹 When used in a query, the view definition replaces the view name  
𖦹 Views can be defined based on other views  

100. Materialized Views  
𖦹 Some systems allow storing view results physically  
𖦹 View is kept updated when base data changes  
- Called materialized view maintenance  
• Can improve performance for frequent queries  
• May become stale if maintenance is delayed  

101. Updating Views Issues  
𖦹 Updates through views must translate to actual base table updates  
𖦹 Some updates may fail or cause missing required data  
• Example: inserting into view without salary leads to null salary or rejection  

102. Complex View Update Problems  
𖦹 Joins in view can make updates ambiguous  
𖦹 Some desired updates cannot be correctly performed through the view  

103. Conditions for Updatable Views (with examples)  
𖦹 <pink>FROM</pink> clause references only one base relation  
• Example (Updatable):  
  <pink>create view</pink> <yellow>cs_instructors as</yellow>  
  <pink>select</pink> <yellow>ID, name, dept_name</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>  
  <pink>where</pink> <yellow>dept_name = 'Comp. Sci'</yellow>;  
- Only one base table is used: instructor  
• Example (Not Updatable):  
  <pink>create view</pink> <yellow>instructor_info as</yellow>  
  <pink>select</pink> <yellow>instructor.ID, name, building</yellow>  
  <pink>from</pink> <yellow>instructor, department</yellow>  
  <pink>where</pink> <yellow>instructor.dept_name = department.dept_name</yellow>;  
- Uses two tables, so updates cannot be done directly  
𖦹 <pink>SELECT</pink> clause has only attribute names (no aggregates or expressions)  
• Example (Updatable):  
  <pink>create view</pink> <yellow>history_instructors as</yellow>  
  <pink>select</pink> <yellow>ID, name, dept_name, salary</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>  
  <pink>where</pink> <yellow>dept_name = 'History'</yellow>;  
- Only attribute names are selected  
• Example (Not Updatable):  
  <pink>create view</pink> <yellow>dept_salary_total as</yellow>  
  <pink>select</pink> <yellow>dept_name, sum(salary)</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>  
  <pink>group by</pink> <yellow>dept_name</yellow>;  
- Uses aggregate <pink>sum</pink><yellow>(salary)</yellow>, so cannot be updated  
𖦹 Attributes not shown must allow nulls and not be part of primary key  
• Example (Updatable if missing attributes allow nulls):  
  <pink>create view</pink> <yellow>instructor_basic as</yellow>  
  <pink>select</pink> <yellow>ID, name</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>;  
- Allowed only if dept_name and salary accept nulls and are not primary key components  
• Example (Not Updatable if missing attributes are NOT NULL or part of key):  
  <pink>create view</pink> <yellow>instructor_without_id as</yellow>  
  <pink>select</pink> <yellow>name, dept_name</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>;  
- Cannot insert because ID is primary key and required, so missing attribute breaks rule  
𖦹 No <pink>GROUP BY</pink> or <pink>HAVING</pink>  
• Example (Updatable):  
  <pink>create view</pink> <yellow>bio_instructors as</yellow>  
  <pink>select</pink> <yellow>*</yellow>  
  <pink>from</pink> <yellow>instructor</yellow>  
  <pink>where</pink> <yellow>dept_name = 'Biology'</yellow>;  
- Simple selection, no grouping  
• Example (Not Updatable):  
  <pink>create view</pink> <yellow>building_instructor_count as</yellow>  
  <pink>select</pink> <yellow>building, count(ID)</yellow>  
  <pink>from</pink> <yellow>department, instructor</yellow>  
  <pink>where</pink> <yellow>instructor.dept_name = department.dept_name</yellow>  
  <pink>group by</pink> <yellow>building</yellow>;  
- <pink>GROUP BY</pink> used, view is not updatable  

104. With Check Option  
𖦹 Ensures inserted/updated tuples still satisfy view conditions  
𖦹 If not satisfied, modification is rejected  

105. Alternative: Using Triggers  
𖦹 <pink>Instead of</pink> clause in triggers can handle custom update actions on views  
𖦹 Allows more controlled and logical view-based modifications  

106. Definition of a Transaction  
𖦹 A transaction is a sequence of SQL query and/or update statements  
𖦹 A transaction starts automatically when an SQL statement runs  
𖦹 The transaction ends only when <pink>commit</pink> or <pink>rollback</pink> is issued  

107. Commit Work  
𖦹 <pink>commit work</pink> makes all updates permanent  
𖦹 After <pink>commit</pink>, a new transaction starts automatically  
• Similar to saving changes in a document  

108. Rollback Work  
𖦹 <pink>rollback work</pink> cancels all updates done in the current transaction  
𖦹 Restores the database to state before transaction began  
• Similar to closing a document without saving  

109. Transaction Safety on Failures  
𖦹 If a failure occurs before <pink>commit</pink>, the system rolls back the transaction  
𖦹 Failures may include power loss, errors, or crashes  
• Ensures database does not remain in a partial or inconsistent state  

110. Banking Example of Atomicity  
𖦹 Money transfer updates two accounts: subtract from source, add to destination  
• If system crashes midway, balances may become inconsistent  
- Transactions ensure either both updates happen or none happen  

111. University Application Example  
𖦹 Updating takes and student.tot_cred must occur together  
• If one update happens and the other fails, student data becomes inconsistent  
- Performing both inside a transaction prevents partial updates  

112. Atomicity Concept  
𖦹 A transaction is atomic (indivisible)  
𖦹 Either all changes of a transaction are applied or none  
• Prevents database from being left in a partially updated state  

113. Program Ending Without Commit or Rollback  
𖦹 Behavior depends on implementation  
𖦹 Some systems <pink>commit</pink>, some <pink>roll back</pink>  
• Not standardized in SQL  

114. Autocommit Behavior  
𖦹 Many systems (e.g., MySQL, PostgreSQL) automatically <pink>commit</pink> each statement  
𖦹 Must turn autocommit off to group multiple statements into a transaction  
• Command often: <pink>set autocommit off</pink>  

115. SQL:1999 Transaction Block Syntax  
𖦹 <pink>begin atomic</pink> … <pink>end</pink> encloses multiple statements as one transaction  
𖦹 Only some databases (e.g., SQL Server) support this syntax  

116. Alternative Transaction Control Syntax  
𖦹 Some systems support <pink>begin</pink> to start a transaction  
𖦹 Must explicitly end with <pink>commit work</pink> or <pink>rollback work</pink>  

117. Oracle Commit Behavior  
𖦹 Oracle does not autocommit DML statements by default  
𖦹 If <pink>commit</pink> is not issued, changes are rolled back when disconnecting  
• Local configuration may override default behavior  

118. Integrity Constraints  
𖦹 Integrity constraints ensure data consistency when authorized users modify the database.  
𖦹 They prevent accidental damage to data, unlike security constraints which prevent unauthorized access.  
𖦹 Examples of integrity constraints:  
• An instructor name cannot be null.  
• Instructor IDs must be unique.  
• Every course's department name must match an existing department.  
• A department’s budget must be greater than 0.  
𖦹 In principle, an integrity constraint can be any logical condition on the database, but systems restrict them to efficiently testable constraints.  
𖦹 Integrity constraints are usually defined during schema design and included in <pink>create table</pink> statements.  
𖦹 Constraints can be added later using: <pink>alter table</pink> <yellow>table-name add constraint constraint-definition</yellow>.  
- The DBMS checks whether current data satisfies the constraint before adding.  

119. Constraints on a Single Relation  
𖦹 <pink>create table</pink> can include integrity-constraint statements alongside attribute definitions.  
𖦹 Common constraints available:  
• <pink>not null</pink>  
• <pink>unique</pink>  
• <pink>check</pink><yellow>(predicate)</yellow>  
𖦹 These constraints restrict allowed values for attributes and maintain correctness.  

120. Not Null Constraint  
𖦹 By default, SQL allows null for any attribute unless restricted.  
𖦹 <pink>not null</pink> disallows null values for an attribute.  
• Example: <yellow>name varchar(20) not null</yellow>  
• Example: <yellow>budget numeric(12,2) not null</yellow>  
𖦹 Attempting to insert null into such attributes results in an error.  
𖦹 Primary keys automatically disallow null values; they need not be explicitly marked <pink>not null</pink>.  

121. Unique Constraint  
𖦹 <pink>unique</pink><yellow>(A1, A2, ..., Am)</yellow> specifies that these attributes together must form a superkey.  
𖦹 No two tuples can have the same combination of values for the listed attributes.  
𖦹 Attributes under <pink>unique</pink> may still contain null unless also declared <pink>not null</pink>.  

122. Check Clause  
𖦹 <pink>check</pink><yellow>(predicate)</yellow> ensures that every tuple satisfies the given logical condition.  
• Example: <pink>check</pink><yellow>(budget > 0)</yellow> ensures department budgets are positive.  
• Example with enumerated behavior:  
  <yellow>semester varchar(6)</yellow> <pink>check</pink> <yellow>(semester in ('Fall', 'Winter', 'Spring', 'Summer'))</yellow>  
𖦹 If a <pink>check</pink> expression evaluates to unknown due to null values, the tuple is allowed.  
𖦹 Constraints applying to single attributes are usually placed beside attribute declarations; complex ones appear at the end of <pink>create table</pink>.  

123. Referential Integrity  
𖦹 Ensures that a value in one relation (referencing relation) must exist in another relation (referenced relation).  
𖦹 Declared using <pink>foreign key</pink> clauses.  
• Example: <pink>foreign key</pink> <yellow>(dept name)</yellow> <pink>references</pink> <yellow>department</yellow>  
𖦹 Referenced attributes must form a primary key or <pink>unique</pink> key.  
𖦹 Actions on violation:  
• <pink>on delete cascade</pink>: deleting the referenced tuple deletes referencing tuples.  
• <pink>on update cascade</pink>: updates propagate to referencing relations.  
• <pink>set null</pink> or <pink>set default</pink> may be used instead of cascade.  
𖦹 Foreign key attributes may be null unless explicitly forbidden.  

124. Assigning Names to Constraints  
𖦹 Constraints can be named using <pink>constraint</pink> <yellow>constraint-name</yellow> before the constraint definition.  
• Example: <pink>constraint</pink> <yellow>minsalary</yellow> <pink>check</pink><yellow>(salary > 29000)</yellow>  
𖦹 Named constraints can later be dropped using:  
• <pink>alter table</pink> <yellow>table-name drop constraint constraint-name</yellow>  

125. Constraint Checking During Transactions  
𖦹 Some transactions temporarily violate constraints before later steps restore correctness.  
𖦹 Constraints may be declared initially deferred so they are checked only at the end of the transaction.  
𖦹 Constraints may also be deferrable, meaning they can be switched between immediate and deferred checking using <pink>set constraints</pink>.  

126. Complex Check Conditions and Assertions  
𖦹 <pink>check</pink> predicates can theoretically contain subqueries, but most systems do not support this.  
𖦹 Assertions express global database conditions:  
• <pink>create assertion</pink> <yellow>name</yellow> <pink>check</pink><yellow>(predicate)</yellow>  
𖦹 Assertions are tested at creation and on all updates.  
𖦹 Assertions may impose high performance costs, so few systems support them.  
𖦹 Equivalent behavior can be implemented using triggers.  

127. SQL Data Types and Schemas  
𖦹 SQL supports various built-in and user-defined data types.  
𖦹 Additional date/time types, type conversion, default values, large objects, user-defined types, and key generation are included.  

128. Date and Time Types in SQL  
𖦹 <yellow>date</yellow> stores year, month, and day.  
𖦹 <yellow>time</yellow> stores hour, minute, and second.  
 • <yellow>time(p)</yellow> allows specifying fractional seconds.  
 • <yellow>time with timezone</yellow> stores time-zone data.  
𖦹 <yellow>timestamp</yellow> stores date and time together.  
 • <yellow>timestamp(p)</yellow> allows fractional seconds.  
 • <yellow>timestamp with timezone</yellow> stores time-zone data.  
𖦹 Example values:  
 • <yellow>date '2018-04-25'</yellow>  
 • <yellow>time '09:30:00'</yellow>  
 • <yellow>timestamp '2018-04-25 10:29:01.45'</yellow>  
𖦹 <pink>extract</pink><yellow>(field from d)</yellow> retrieves parts like year, month, hour, etc.  
𖦹 Functions:  
 • <pink>current date</pink> returns current date.  
 • <pink>current time</pink> returns time with timezone.  
 • <pink>localtime</pink> returns time without timezone.  
 • <pink>current timestamp</pink> and <pink>localtimestamp</pink> return date plus time.  
𖦹 Some systems offer <yellow>datetime</yellow> without timezone.  
𖦹 SQL supports arithmetic and comparison on date/time.  
𖦹 <yellow>interval</yellow> type supports calculations like:  
 • <yellow>x - y</yellow> returns days between dates.  
 • Adding/subtracting interval modifies date or time.  

129. Type Conversion and Formatting Functions  
𖦹 Automatic type conversion sometimes occurs; explicit conversion uses <pink>cast</pink><yellow>(e as t)</yellow>.  
𖦹 Casting helps achieve correct ordering or calculations.  
 • Example: <pink>cast</pink><yellow>(ID as numeric(5))</yellow> ensures numeric sorting.  
𖦹 Formatting functions change output display but not stored data.  
 • MySQL: <pink>format</pink>  
 • Oracle/PostgreSQL: <pink>to char</pink>, <pink>to number</pink>, <pink>to date</pink>  
 • SQL Server: <pink>convert</pink>  
𖦹 <pink>coalesce</pink> returns first non-null value.  
 • Example: <pink>coalesce</pink><yellow>(salary, 0)</yellow> displays 0 instead of null.  
𖦹 <pink>decode</pink> (Oracle) replaces values based on matching patterns.  
 • Can replace null with 'N/A' when needed.  

130. Default Values  
𖦹 Attributes can have default values in table definitions.  
𖦹 Example:  
 • <yellow>tot cred numeric(3,0) default 0</yellow>  
𖦹 Inserting without specifying tot cred assigns default value automatically.  

131. Large-Object Types  
𖦹 <yellow>clob</yellow> stores large character data.  
𖦹 <yellow>blob</yellow> stores large binary data (images, video, etc.).  
𖦹 Example:  
 • <yellow>book review clob(10KB)</yellow>  
 • <yellow>image blob(10MB)</yellow>  
 • <yellow>movie blob(2GB)</yellow>  
𖦹 Large objects are usually accessed through locators rather than loading all data at once.  

132. User-Defined Types (Distinct Types)  
𖦹 <pink>create type</pink> defines custom types separate from built-in types.  
𖦹 Example:  
 • <pink>create type</pink> <yellow>Dollars as numeric(12,2) final</yellow>;  
𖦹 Used to prevent errors, such as mixing currency types.  
𖦹 Operations between different distinct types require explicit <pink>cast</pink>.  
𖦹 <pink>drop type</pink> and <pink>alter type</pink> modify or remove user-defined types.  

133. Domains  
𖦹 <pink>create domain</pink> defines new domains with constraints.  
𖦹 Example:  
 • <pink>create domain</pink> <yellow>DDollars as numeric(12,2) not null</yellow>;  
𖦹 Domains support constraints and default values.  
𖦹 Domains are not strictly typed; compatible types may mix.  
𖦹 Example constraint:  
 • <pink>create domain</pink> <yellow>YearlySalary numeric(8,2)</yellow>  
   <pink>check</pink><yellow>(value >= 29000.00)</yellow>;  
𖦹 Domains can also restrict values using <pink>in</pink> clause.  

134. Generating Unique Key Values  
𖦹 Automatically assigns unique numeric identifiers.  
𖦹 Example syntax:  
 • <yellow>ID number(5) generated always as identity</yellow>  
𖦹 <pink>always</pink> means user cannot manually insert value.  
𖦹 <pink>by default</pink> allows manual insertion or automatic generation.  
𖦹 Equivalent features:  
 • PostgreSQL: <yellow>serial</yellow>  
 • MySQL: <yellow>auto increment</yellow>  
 • SQL Server: <yellow>identity</yellow>  
𖦹 <pink>create sequence</pink> generates unique values for multiple tables.  

135. Create Table Extensions  
𖦹 <pink>create table</pink> ... <pink>like</pink> duplicates schema.  
 • Example: <pink>create table</pink> <yellow>temp instructor like instructor</yellow>;  
𖦹 <pink>create table</pink> ... <pink>as</pink> stores query results into a new table.  
 • Example:  
   <pink>create table</pink> <yellow>t1 as (</yellow><pink>select</pink> <yellow>*</yellow> <pink>from</pink> <yellow>instructor</yellow> <pink>where</pink> <yellow>dept name = 'Music')</yellow> <pink>with data</pink>;  
𖦹 Table created with data stores current results; views reflect dynamic data.  
136. Schemas, Catalogs, and Environments  
𖦹 SQL uses a hierarchy: catalogs contain schemas, schemas contain tables and objects.  
𖦹 Similar to folders and subfolders in file systems.  
𖦹 Users connect to a specific catalog before performing operations.  

137. Authorization  
𖦹 Users can be given specific rights called privileges.  
𖦹 Data privileges include:  
• Read data (<pink>select</pink>)  
• Insert new data (<pink>insert</pink>)  
• Update data (<pink>update</pink>)  
• Delete data (<pink>delete</pink>)  
𖦹 A user may receive any combination of privileges on specific relations or views.  
𖦹 When a query or update is submitted, the system checks if the user has proper authorization.  
𖦹 Users may also be granted schema permissions to create, modify, or drop relations.  
𖦹 Some users may further grant or revoke privileges from others.  
𖦹 The database administrator has full authority over all users and schema objects.  

138. Granting and Revoking of Privileges  
𖦹 SQL privileges include <pink>select</pink>, <pink>insert</pink>, <pink>update</pink>, and <pink>delete</pink>.  
𖦹 <pink>all privileges</pink> can be used to grant all allowed privileges at once.  
𖦹 A user who creates a relation automatically gets all privileges on it.  
𖦹 Privileges are granted using:  
• <pink>grant</pink> <yellow><privileges> on <relation/view> to <user/role list></yellow>;  
𖦹 Example:  
• <pink>grant select on</pink> <yellow>department to Amit, Satoshi</yellow>; (allows reading)  
𖦹 <pink>update</pink> privilege can be given on all attributes or specific attributes:  
• <pink>grant update</pink><yellow>(budget)</yellow> <pink>on</pink> <yellow>department to Amit, Satoshi</yellow>;  
𖦹 <pink>insert</pink> privilege may specify attributes; missing attributes get default or null values.  
𖦹 <pink>delete</pink> privilege allows removal of tuples.  
𖦹 <yellow>public</yellow> refers to all users.  
𖦹 To revoke privileges:  
• <pink>revoke</pink> <yellow><privileges> on <relation/view> from <user/role list></yellow>;  

139. Roles  
𖦹 Roles allow grouping privileges and assigning them to multiple users.  
𖦹 Suitable where many users need the same permissions.  
𖦹 Users are assigned roles; roles store the actual privileges.  
𖦹 Roles prevent security issues caused by shared user accounts.  
𖦹 Roles are created using:  
• <pink>create role</pink> <yellow>instructor</yellow>;  
𖦹 Privileges are granted to roles like users:  
• <pink>grant select on</pink> <yellow>takes to instructor</yellow>;  
𖦹 Roles can be granted to users or other roles:  
• <pink>grant</pink> <yellow>instructor to dean</yellow>;  
• <pink>grant</pink> <yellow>dean to Satoshi</yellow>;  
𖦹 A user’s effective privileges include:  
• Privileges granted directly  
• Privileges from assigned roles (including indirectly inherited roles)  

140. Authorization on Views  
𖦹 Users may be given privileges on views to restrict which parts of a relation they can see.  
𖦹 The system checks authorization before expanding a view into its underlying relations.  
𖦹 A user must already have required privileges on base relations to receive certain privileges on a view.  
𖦹 Example: View for Geology department faculty:  
• <pink>create view</pink> <yellow>geo_instructor as (</yellow><pink>select</pink> <yellow>*</yellow> <pink>from</pink> <yellow>instructor</yellow> <pink>where</pink> <yellow>dept_name = 'Geology'</yellow>);  
𖦹 If the user lacks <pink>select</pink> on <yellow>instructor</yellow>, the system denies creation of such a view.  
𖦹 Functions and procedures can also have <pink>execute</pink> privilege.  
𖦹 <pink>sql security invoker</pink> causes a function to run under the caller’s privileges instead of the creator’s.  

141. Authorization on Schema  
𖦹 Schema modifications such as creating or deleting relations are allowed only by the schema owner.  
𖦹 <pink>references</pink> privilege is required to declare foreign keys referencing another relation.  
• Example:  
<pink>grant references</pink><yellow>(dept_name)</yellow> <pink>on</pink> <yellow>department to Mariano</yellow>;  
𖦹 Foreign keys and <pink>check</pink> constraints referencing another relation may limit updates, so <pink>references</pink> privilege is needed.  

142. Transfer of Privileges  
𖦹 A user granted a privilege may or may not be allowed to grant it further.  
𖦹 To allow granting to others, use <pink>with grant option</pink>:  
• <pink>grant select on</pink> <yellow>department to Amit with grant option</yellow>;  
𖦹 Privileges can be represented as an authorization graph showing who granted privileges to whom.  

143. Revoking of Privileges  
𖦹 Revocation may cause others to lose privileges if those privileges were passed on.  
𖦹 This behavior is called cascading revocation.  
𖦹 Example:  
• <pink>revoke select on</pink> <yellow>department from Amit, Satoshi</yellow>;  
𖦹 If <pink>restrict</pink> is specified, <pink>revoke</pink> fails if cascading is required:  
• <pink>revoke select on</pink> <yellow>department from Amit restrict</yellow>;  
𖦹 <pink>grant option</pink> can be revoked without removing the privilege itself.  

144. Row-Level Authorization  
𖦹 Some systems allow controlling access to specific tuples.  
𖦹 Oracle VPD attaches a predicate to a relation so only permitted rows are returned.  
𖦹 Example predicate:  
• <yellow>ID = sys_context('USERENV','SESSION_USER')</yellow>  
𖦹 This ensures users see only their own rows.  
𖦹 May change meaning of queries if not carefully designed (for example, aggregates apply only to visible rows).  

145. Accessing SQL from a Programming Language  
𖦹 SQL is a powerful declarative query language, easier than general-purpose languages.  
𖦹 General-purpose languages are needed because:  
• Some queries cannot be expressed in SQL alone.  
- Embedding SQL in languages like C, Java, or Python solves this.  
• Nondeclarative actions like printing reports or GUI interactions cannot be done in SQL.  
- Applications often combine SQL with general-purpose languages.  
𖦹 Two approaches to access SQL from a programming language:  
• Dynamic SQL: Queries constructed at runtime as strings, executed, and results retrieved.  
• Embedded SQL: SQL statements identified at compile time, translated by a preprocessor.  

146. Standards for Connecting to SQL  
𖦹 JDBC: Java API for connecting to databases.  
𖦹 ODBC: Originally for C, extended to many languages like C++, C#, Ruby, Go, PHP.  
𖦹 Python Database API: Allows Python programs to connect to databases.  
𖦹 ADO.NET: For Visual Basic .NET and C#, similar to JDBC, can access non-relational sources.  

147. JDBC Overview  
𖦹 JDBC allows Java programs to connect to database servers.  
𖦹 Requires importing <yellow>java.sql.*</yellow>.  
𖦹 Steps to use JDBC:  
• Connect to database using <yellow>DriverManager.getConnection()</yellow>.  
• Use <yellow>Statement</yellow> object to send SQL statements.  
• Retrieve results via <yellow>ResultSet</yellow>.  
• Handle exceptions and manage resources.  

148. Connecting to the Database  
𖦹 <yellow>getConnection()</yellow> parameters:  
• URL of the server, protocol, port, and database name.  
• Database user identifier (string).  
• Password (string).  
𖦹 <yellow>Connection</yellow> object handle is created for interaction.  
𖦹 JDBC driver must be loaded to translate JDBC calls to database-specific calls.  

149. Shipping SQL Statements  
𖦹 Use <yellow>Statement</yellow> object to execute SQL.  
• <yellow>executeQuery()</yellow> for queries returning <yellow>ResultSet</yellow>.  
• <yellow>executeUpdate()</yellow> for <pink>insert</pink>, <pink>update</pink>, <pink>delete</pink>, <pink>create table</pink>; returns number of affected tuples.  
𖦹 DDL statements return zero.  

150. Exceptions and Resource Management  
𖦹 Use <yellow>try-catch</yellow> to handle <yellow>SQLExceptions</yellow> and general <yellow>Exceptions</yellow>.  
𖦹 Resources like connections and statements must be closed to avoid exhaustion.  
𖦹 Prefer <yellow>try-with-resources</yellow> in Java to automatically close resources.  

151. Retrieving Query Results  
𖦹 <yellow>ResultSet</yellow> fetches tuples one at a time.  
• <yellow>next()</yellow> method checks and retrieves next tuple.  
• <yellow>getString()</yellow>, <yellow>getFloat()</yellow>, etc., retrieve attribute values by name or position.  

152. Prepared Statements  
𖦹 <yellow>PreparedStatement</yellow> allows parameterized queries with “?” placeholders.  
𖦹 Parameters set using methods like <yellow>setString()</yellow> and <yellow>setInt()</yellow>.  
𖦹 Advantages:  
• Efficient execution of repeated queries.  
• Prevents SQL injection by escaping user input.  
𖦹 Never concatenate user input directly into SQL statements.  

153. Callable Statements  
𖦹 <yellow>CallableStatement</yellow> invokes SQL stored procedures and functions.  
𖦹 <yellow>registerOutParameter()</yellow> registers return types and out parameters.  
𖦹 <yellow>get</yellow> methods retrieve results similar to <yellow>ResultSet</yellow>.  
154. Metadata Features  
𖦹 <yellow>ResultSetMetaData</yellow> provides info about result sets:  
• Number of columns, column names, types.  
𖦹 <yellow>DatabaseMetaData</yellow> provides info about database and system:  
• Tables, columns, primary keys, foreign keys, authorizations, limits.  
𖦹 Metadata allows writing generic programs without hard-coded schema.  

155. Other JDBC Features  
𖦹 Updatable result sets allow updating tuples in database via result sets.  
𖦹 Transaction control:  
• <yellow>setAutoCommit(false)</yellow> disables automatic commit.  
• <yellow>commit()</yellow> and <yellow>rollback()</yellow> control transactions manually.  
𖦹 Handling large objects:  
• <yellow>getBlob()</yellow>, <yellow>getClob()</yellow> fetch locators to large objects.  
• <yellow>setBlob()</yellow>, <yellow>setClob()</yellow> store data from streams.  
𖦹 Row sets can be collected, shipped, scanned, and modified.  

156. Database Access from Python  
𖦹 Python can access databases using drivers like <yellow>psycopg2</yellow> for PostgreSQL.  
𖦹 Prepared statements in Python use <yellow>"%s"</yellow> for parameters and require <yellow>commit()</yellow> to save updates.  
• Parameter values are provided as a list.  
- Updates are not committed automatically; <yellow>commit()</yellow> must be called.  
𖦹 Exceptions are caught using <yellow>try-except</yellow> blocks.  
• Provides information about errors.  
𖦹 Query results can be iterated using a <yellow>for</yellow> loop.  
• Individual row attributes can be accessed via indexing.  
𖦹 Different drivers exist for different databases: <yellow>MySQLdb</yellow> for MySQL, <yellow>cx_Oracle</yellow> for Oracle, <yellow>pyodbc</yellow> for ODBC.  
• Minor differences exist across drivers in the <yellow>connect()</yellow> function parameters.  

157. ODBC  
𖦹 ODBC defines an API to connect to databases, send queries, and receive results.  
𖦹 Each database provides a library that communicates with the server when API calls are made.  
𖦹 Steps to use ODBC in C:  
• Allocate SQL environment (<yellow>HENV</yellow>), then connection handle (<yellow>HDBC</yellow>).  
• Open connection using <yellow>SQLConnect</yellow> with server, user, and password.  
• Send SQL commands using <yellow>SQLExecDirect</yellow>.  
• Bind C variables to query attributes using <yellow>SQLBindCol</yellow>.  
• Fetch result tuples using <yellow>SQLFetch</yellow> in a loop.  
• Free statement handle, disconnect, and free environment at the end.  
𖦹 SQL statements can use placeholders <yellow>"?"</yellow> for parameters.  
• Statements can be prepared and executed multiple times with different values.  
𖦹 Automatic commit is on by default.  
• Turn off with <yellow>SQLSetConnectOption(conn, SQL_AUTOCOMMIT, 0)</yellow>.  
• Transactions can be committed with <yellow>SQLTransact(conn, SQL_COMMIT)</yellow> or rolled back with <yellow>SQLTransact(conn, SQL_ROLLBACK)</yellow>.  
𖦹 ODBC defines conformance levels:  
• Core: basic functionality.  
• Level 1: fetch catalog info like relations and attribute types.  
• Level 2: advanced features like arrays of parameter values and detailed catalog info.  
𖦹 SQL Call Level Interface (CLI) is similar to ODBC.  

158. Embedded SQL  
𖦹 SQL can be embedded in host languages like C, C++, Java, COBOL, Pascal, PL/I, and Fortran.  
𖦹 A preprocessor converts embedded SQL into host-language code before compilation.  
• <yellow>EXEC SQL <statement>;</yellow> identifies embedded SQL.  
𖦹 Host language variables can be used in SQL with a colon prefix (<yellow>:</yellow>).  
𖦹 Cursors allow iteration over query results.  
• Fetch rows into host language variables.  
• Updates can be performed using cursors with optional <pink>WHERE</pink> clauses.  
𖦹 Embedded SQL allows some SQL errors to be caught at preprocessing.  
𖦹 Queries in embedded SQL are easier to read than dynamic SQL.  
𖦹 Disadvantages:  
• Preprocessor generates extra host code, complicating debugging.  
• Syntax clashes with host language may occur in future versions.  
𖦹 Most modern systems prefer dynamic SQL.  
• Microsoft LINQ is an exception, integrating queries into the host language.  

159. Embedded Databases  
𖦹 Embedded databases exist entirely within an application.  
• Accessible only through the application.  
𖦹 Examples include Java DB, SQLite, HSQLDB, and embedded MySQL.  
𖦹 They lack full server features but are suitable for small databases or internal applications.  
𖦹 Embedded databases are different from embedded SQL.  
• Embedded SQL connects to server-based databases, not internal-only databases.  

160. Functions and Procedures  
𖦹 SQL allows developers to write their own functions and procedures and store them in the database.  
𖦹 Functions are useful with specialized data types like images or geometric objects.  
• Example: line-segment overlap check in a map database, image similarity functions.  
𖦹 Procedures and functions allow business logic to be stored in the database.  
• Example: rules for course limits, instructor teaching load, maximum majors for a student.  
- Advantages: multiple applications can access them, single point of change for business rules.  
𖦹 SQL allows definitions in SQL or external languages like Java, C, or C++.  
𖦹 Most databases implement nonstandard syntax, e.g., Oracle PL/SQL, SQL Server T-SQL, PostgreSQL PL/pgSQL.  
- Concepts remain applicable despite syntax differences.  

161. Declaring and Invoking SQL Functions and Procedures  
𖦹 Functions can return values or tables and be used in SQL queries.  
• Example: <yellow>dept_count</yellow> function returns the number of instructors in a department.  
• Table functions return a set of rows like parameterized views.  
𖦹 Complex user-defined functions can cause performance issues if applied to many tuples.  
𖦹 Procedures can have <pink>IN</pink> and <pink>OUT</pink> parameters.  
• <pink>IN</pink>: input values; <pink>OUT</pink>: values returned by the procedure.  
𖦹 Procedures can be called from SQL, embedded SQL, or dynamic SQL (e.g., JDBC).  
𖦹 SQL supports multiple procedures/functions with the same name if argument number or types differ.  

162. Language Constructs for Procedures and Functions  
𖦹 SQL supports general-purpose programming constructs in procedures/functions.  
𖦹 Variables declared using <pink>declare</pink>, assigned with <pink>set</pink>.  
𖦹 Compound statements: <pink>begin</pink> … <pink>end</pink>; <pink>begin atomic</pink> … <pink>end</pink> ensures single transaction.  
𖦹 Loops: <pink>while</pink>, <pink>repeat</pink>, <pink>for</pink> over query results.  
• <pink>Leave</pink> exits loop; <pink>iterate</pink> moves to next tuple.  
𖦹 Conditional statements: <pink>if-then-elseif-else</pink>, <pink>case</pink> statements.  
𖦹 Exception handling:  
• <pink>Declare</pink> conditions and handlers.  
• <pink>signal</pink> raises exceptions; handlers specify actions (<pink>exit</pink> or <pink>continue</pink>).  
• Predefined conditions: <pink>sqlexception</pink>, <pink>sqlwarning</pink>, <pink>not found</pink>.  
𖦹 Example: <yellow>registerStudent</yellow> function registers a student in a course section, checks capacity, returns success/error codes.  

163. External Language Routines  
𖦹 SQL procedural features vary across databases; syntax is not standardized.  
𖦹 Alternative: define procedures/functions in an external language (Java, C#, C, C++).  
• More efficient; can perform computations not possible in SQL.  
𖦹 Syntax example: <pink>create procedure/function in C with external file path</pink>.  
𖦹 External routines must handle null values, return values, exceptions.  
• Extra parameters like <yellow>sqlstate</yellow>, return value indicators may be needed.  
𖦹 Risks of external routines:  
• Compiled outside database may corrupt internal structures or bypass access control.  
𖦹 Execution strategies:  
• Unsafe languages (C) may require separate process, causing overhead from interprocess communication.  
• Safe languages (Java, C#) can run in sandbox within database process, reducing overhead.  
𖦹 Modern databases supporting sandboxed external routines:  
• Oracle, IBM DB2: Java functions within database process.  
• SQL Server CLR: C# or VB procedures.  
• PostgreSQL: functions in Perl, Python, Tcl.  

164. Triggers  
𖦹 A trigger is a statement executed automatically as a side effect of a database modification.  
𖦹 To define a trigger:  
• Specify the event and condition for execution.  
• Specify the actions to be taken when executed.  
𖦹 Once entered, the database executes the trigger whenever the event occurs and condition is satisfied.  

165. Need for Triggers  
𖦹 Triggers implement integrity constraints not possible with standard SQL constraints.  
𖦹 Useful for alerting humans or starting tasks automatically.  
• Example: update student total credits when a course is taken.  
• Example: reorder inventory automatically when it falls below minimum.  
𖦹 Triggers cannot usually perform external updates; separate processes handle actions outside the database.  
• Some systems support sending emails from SQL queries or triggers.  

166. Triggers in SQL  
𖦹 SQL standard defines triggers; most databases have nonstandard syntax.  
𖦹 Triggers ensure referential integrity using <pink>after insert</pink>, <pink>delete</pink>, or <pink>update</pink> events.  
• Transition variables (<yellow>referencing new row/old row</yellow>) store affected row values.  
• <pink>when</pink> clause specifies the condition for trigger execution.  
• <pink>begin atomic</pink> … <pink>end</pink> groups multiple SQL statements.  
𖦹 Example: <pink>rollback transaction</pink> if inserted time slot id is invalid.  
𖦹 Triggers can execute on updates to specific attributes.  
• Example: <pink>update</pink> <yellow>student tot_cred</yellow> when grade changes from null or 'F' to a passing grade.  
𖦹 Triggers can be <pink>before</pink> or <pink>after</pink> events.  
• <pink>Before</pink> triggers can correct or prevent invalid actions.  
• Example: replace blank grades with null using <pink>set</pink> statement.  
𖦹 Statement-level triggers operate once per SQL statement; row-level triggers operate for each affected row.  
𖦹 Triggers can be enabled, disabled, or dropped.  

167. Inventory Replenishment Example  
𖦹 Relations: <yellow>inventory(item, level)</yellow>, <yellow>minlevel(item, level)</yellow>, <yellow>reorder(item, amount)</yellow>, <yellow>orders(item, amount)</yellow>.  
𖦹 Trigger placed to reorder when inventory falls below minimum level.  
• Ensures order placed only when level falls from above minimum to below minimum.  
𖦹 SQL-based systems widely use triggers; standard syntax varies across systems (pre-SQL:1999 triggers were not standard).  

168. When Not to Use Triggers  
𖦹 Some tasks are better handled by other techniques than triggers.  
• Example: <pink>on delete cascade</pink> feature is simpler with foreign-key cascade than with triggers.  
𖦹 Materialized views can be maintained automatically by the database, reducing need for triggers.  
𖦹 Triggers can be used for database replication, but modern systems provide built-in replication features.  

169. Special Considerations for Triggers  
𖦹 Triggers may cause unintended execution during data loading or replication.  
• Can be disabled during backup load or use <yellow>"not for replication"</yellow> options.  
𖦹 Triggers must be written carefully to avoid runtime errors and infinite chains of triggers.  
• Some databases limit chain length (e.g., 16 or 32 triggers) or flag errors when a trigger references its own table.  
𖦹 Triggers are useful but should be avoided when alternatives exist.  

170. Recursive Queries  
𖦹 Recursive queries find all direct and indirect prerequisites for a course.  
• Example: CS-347 has CS-319 as direct prerequisite; CS-315 and CS-101 are prerequisites for CS-319; CS-190 is prerequisite for CS-315; CS-101 already listed.  
𖦹 Transitive closure contains all pairs (course, prerequisite) for direct or indirect prerequisites.  
• Useful for hierarchies like organizational units or machine parts.  

171. Transitive Closure Using Iteration  
𖦹 Iterative approach finds direct prerequisites, then prerequisites of those courses, until no new courses are added.  
𖦹 Function <yellow>findAllPrereqs(cid)</yellow> computes all prerequisites for a given course.  
• Uses three temporary tables: <yellow>c_prereq (results)</yellow>, <yellow>new_c_prereq (previous iteration)</yellow>, <yellow>temp (intermediate storage)</yellow>.  
• Inserts direct prerequisites into <yellow>new_c_prereq</yellow>, then repeats:  
  - Add <yellow>new_c_prereq</yellow> to <yellow>c_prereq</yellow>.  
  - Find prerequisites of courses in <yellow>new_c_prereq</yellow> not already in <yellow>c_prereq</yellow> and store in <yellow>temp</yellow>.  
  - Replace <yellow>new_c_prereq</yellow> with <yellow>temp</yellow> and clear <yellow>temp</yellow>.  
• Loop ends when <yellow>new_c_prereq</yellow> is empty.  
𖦹 The <pink>except</pink> clause prevents duplicates and handles cycles in prerequisites.  
• Applicable to other recursive relationships like flight reachability.  

172. Recursion in SQL  
𖦹 Recursive views define direct and indirect prerequisites in a single query.  
• Example: <pink>with recursive</pink> <yellow>rec_prereq(course_id, prereq_id) as (base query union recursive query)</yellow> <pink>select</pink> <yellow>*</yellow> <pink>from</pink> <yellow>rec_prereq</yellow>.  
𖦹 Base query selects direct prerequisites; recursive query joins <yellow>rec_prereq</yellow> with <yellow>prereq</yellow> to find indirect prerequisites.  
𖦹 Recursive view iteratively adds tuples until a fixed point is reached (no new tuples).  
𖦹 To find prerequisites of a specific course, add a <pink>where</pink> clause to filter <yellow>course_id</yellow>.  
𖦹 Database systems may optimize execution without computing full <yellow>rec_prereq</yellow> first.  

173. Restrictions on Recursive Queries  
𖦹 Recursive queries must be monotonic.  
• Result on a larger view instance must include all results from a smaller instance.  
𖦹 Nonmonotonic constructs are not allowed:  
• Aggregation on recursive view.  
• <pink>not exists</pink> on a subquery using recursive view.  
• Set difference (<pink>except</pink>) with right-hand side using recursive view.  
𖦹 Nonmonotonic queries make the meaning of the view hard to define; SQL requires monotonic queries.  

174. Additional Notes on Recursive Queries  
𖦹 SQL supports permanent recursively defined views using <pink>create recursive view</pink>.  
𖦹 Some databases use alternative syntax for recursion (e.g., Oracle <pink>start with</pink> / <pink>connect by prior</pink> for hierarchical queries).  
• Oracle 12.c supports standard syntax with <pink>union all</pink> and <pink>recursive</pink> keyword omitted.  

175. Advanced Aggregation Features  
𖦹 SQL aggregation is powerful but basic features can be inefficient for some tasks.  
𖦹 Advanced features help handle ranking, windowing, pivoting, rollup, and cube operations efficiently.  

176. Ranking  
𖦹 Ranking finds the position of a value within a set, e.g., student GPA ranks.  
• <pink>rank()</pink> assigns the same rank to equal values and skips ranks for ties.  
• <pink>dense_rank()</pink> assigns consecutive ranks without gaps.  
• <pink>nulls first/last</pink> controls how nulls are ranked.  
• SQL example: <pink>select</pink> <yellow>ID,</yellow> <pink>rank()</pink> <yellow>over (order by GPA desc)</yellow> <pink>as</pink> <yellow>s_rank</yellow> <pink>from</pink> <yellow>student_grades</yellow> <pink>order by</pink> <yellow>s_rank</yellow>;  
𖦹 Ranking can be done within partitions (e.g., department):  
• Use <pink>partition by</pink>: <pink>select</pink> <yellow>ID, dept_name,</yellow> <pink>rank()</pink> <yellow>over (partition by dept_name order by GPA desc)</yellow> <pink>as</pink> <yellow>dept_rank</yellow> <pink>from</pink> <yellow>dept_grades</yellow> <pink>order by</pink> <yellow>dept_name, dept_rank</yellow>;  
𖦹 Top n ranking can be obtained using a containing query with <pink>where</pink> clause.  
𖦹 Nonstandard SQL syntax exists for top n queries but may break ties arbitrarily.  
𖦹 Other ranking functions:  
• <pink>percent_rank</pink> = <yellow>(r-1)/(n-1)</yellow>  
• <pink>cume_dist</pink> = <yellow>p/n</yellow> (cumulative distribution)  
• <pink>row_number</pink> = unique number for each row  
𖦹 <pink>ntile(n)</pink> divides tuples into n equal buckets.  

177. Windowing  
𖦹 Window queries compute aggregates over ranges of tuples.  
• Useful for trend analysis and moving averages.  
𖦹 Example with fixed range:  
• <pink>select</pink> <yellow>year, avg(num_credits)</yellow> <pink>over (order by year rows 3 preceding)</pink> <pink>as</pink> <yellow>avg_total_credits</yellow> <pink>from</pink> <yellow>tot_credits</yellow>;  
𖦹 Example with all prior years:  
• <pink>select</pink> <yellow>year, avg(num_credits)</yellow> <pink>over (order by year rows unbounded preceding)</pink> <pink>as</pink> <yellow>avg_total_credits</yellow> <pink>from</pink> <yellow>tot_credits</yellow>;  
𖦹 Windows can start and end before or after current row:  
• <yellow>rows between 3 preceding and 2 following</yellow>  
𖦹 Partitioning allows separate windows for each group:  
• <pink>select</pink> <yellow>dept_name, year, avg(num_credits)</yellow> <pink>over (partition by dept_name order by year rows between 3 preceding and current row)</pink> <pink>as</pink> <yellow>avg_total_credits</yellow> <pink>from</pink> <yellow>tot_credits_dept</yellow>;  
𖦹 <pink>range</pink> keyword covers all tuples with a specific sort attribute value.  

178. Pivoting  
𖦹 Pivoting transforms rows into columns (cross-tabulation).  
• Example: <yellow>sales(item_name, color, clothes_size, quantity)</yellow> → pivot on color.  
𖦹 SQL pivot example:  
• <pink>select</pink> <yellow>*</yellow> <pink>from</pink> <yellow>sales</yellow> <pink>pivot</pink> <yellow>(sum(quantity) for color in ('dark','pastel','white'))</yellow>;  
𖦹 Pivot attributes are renamed to values of another attribute.  
𖦹 Aggregate functions handle multiple tuples per cell (e.g., <pink>sum</pink>).  

179. Rollup and Cube  
𖦹 Rollup generates hierarchical aggregates for multiple groupings.  
• Example: <pink>group by</pink> <yellow>rollup(item_name, color)</yellow> → groupings: (item_name,color), (item_name), ()  
• Nulls represent missing attributes in higher-level groupings.  
𖦹 Cube generates all combinations of groupings.  
• Example: <pink>group by</pink> <yellow>cube(item_name,color,clothes_size)</yellow> → 8 groupings including empty.  
𖦹 Multiple rollups and cubes can be combined.  
𖦹 For custom groupings, use grouping sets:  
• <pink>group by</pink> <yellow>grouping_sets((color,clothes_size),(clothes_size,item_name))</yellow>;  
𖦹 Distinguish rollup/cube-generated nulls using <pink>grouping()</pink> function:  
• <pink>case</pink> <pink>when</pink> <pink>grouping</pink><yellow>(item_name)=1</yellow> <pink>then</pink> <yellow>'all'</yellow> <pink>else</pink> <yellow>item_name</yellow> <pink>end</pink>  
𖦹 <pink>coalesce</pink> cannot differentiate rollup/cube nulls from normal nulls.

180. Overview of the Design Process
𖦹 Creating a database application involves designing the database schema, programs that access/update data, and a security scheme.
𖦹 User needs are central to the design process.
𖦹 Focus here is on designing the database schema; other design tasks are briefly outlined.

181. Design Phases
𖦹 Small applications may allow direct design of relations, attributes, and constraints.
𖦹 Real-world applications are complex; no one person may understand all data needs.
𖦹 Database designer must interact with users, represent needs in a high-level model, and translate to lower-level design.
• High-level data model provides a conceptual framework to specify data requirements and structure.
𖦹 Initial phase: characterize fully the data needs of users.
• Interact extensively with domain experts.
• Outcome: specification of user requirements, usually textual.
𖦹 Next: choose a data model and translate requirements into a conceptual schema.
• Conceptual schema gives a detailed overview of the enterprise.
• E-R model represents entities, attributes, relationships, and constraints.
• Usually results in an entity-relationship diagram (ERD).
𖦹 Review schema to confirm all data requirements are satisfied and remove redundancy.
𖦹 Conceptual schema also indicates functional requirements, such as operations on data (update, search, delete).
𖦹 Moving from abstract model to implementation:
• Logical-design phase: map conceptual schema to implementation data model (usually relational).
• Physical-design phase: specify physical features like file organization and indexes.
𖦹 Physical schema can change easily; logical schema changes are harder and affect queries and updates.

182. Design Alternatives
𖦹 Database design requires deciding how to represent entities like people, places, products.
• Entity: distinctly identifiable item (e.g., instructors, students, courses, course offerings/sections).
𖦹 Entities are related in various ways (e.g., student takes course, instructor teaches course).
𖦹 Avoid two major pitfalls:
1. Redundancy:
• Repeating information unnecessarily (e.g., storing course title with each section).
- Can lead to inconsistencies when updates occur.
2. Incompleteness:
• Difficult or impossible to model certain aspects if design lacks entities (e.g., course entity missing).
- Workarounds like nulls are unattractive and may conflict with primary key constraints.
𖦹 Multiple good designs exist; choices affect modeling quality.
• Example: representing a sale as a relationship or an entity.

183. The Entity-Relationship Model
𖦹 E-R model helps specify an enterprise schema representing the logical structure of a database.
𖦹 Useful for mapping real-world enterprises to a conceptual schema.
𖦹 Three basic concepts: entity sets, relationship sets, attributes.
𖦹 E-R diagram provides a clear graphical representation of the schema.
𖦹 Tools exist for creating E-R diagrams.
IMG_URL er-diagram.png

184. Entity Sets
𖦹 Entity: distinguishable real-world object (e.g., person, course).
𖦹 Each entity has properties; some values uniquely identify it (e.g., person id, course id).
𖦹 Entity set: collection of entities of the same type sharing attributes (e.g., all instructors, all students).
𖦹 Extension: actual collection of entities in an entity set.
𖦹 Entity sets may overlap (e.g., a person may be both student and instructor).
𖦹 Attributes describe each entity; values can differ per entity (e.g., ID, name, dept name, salary).
𖦹 Simple attributes considered here; composite/multivalued discussed later.
𖦹 Primary key attribute uniquely identifies each entity (underlined in ERD).
𖦹 Entity sets represented in ERD as rectangles with entity name and attributes.
IMG_URL entity-set.png

185. Relationship Sets
𖦹 Relationship: association among entities (e.g., advisor associates instructor Katz with student Shankar).
𖦹 Relationship set: collection of relationships of the same type.
IMG_URL relationship-set.png
𖦹 Relationship instance: specific association between named entities.
𖦹 Represented in ERD as diamonds linked to entity sets (rectangles).
𖦹 Relationships may involve more than two entity sets.
𖦹 Participation: entity sets involved in a relationship set.
𖦹 Role: function an entity plays in a relationship; explicit role names needed in recursive relationships.
• Example: prereq relationship between courses C1 and C2.
IMG_URL role-indicators.png
𖦹 Relationship sets can have descriptive attributes (e.g., grade in takes relationship).
• Attributes represented by undivided rectangles linked with dashed lines to diamonds.
IMG_URL er-with-attribute.png
𖦹 Complex ER designs may require multiple diagrams; entity sets may repeat without attributes to avoid redundancy.
𖦹 Nonbinary relationship sets exist; degree = number of entity sets involved.
• Example: ternary relationship proj guide among instructor, student, and project.
• Instance indicates which student is guided by which instructor on which project.

186. Complex Attributes
𖦹 Each attribute has a set of permitted values called the domain or value set.
• Example: course id domain = all text strings of certain length; semester domain = {Fall, Winter, Spring, Summer}.
𖦹 Attributes can be classified into different types.
IMG_URL complex-attributes.png

187. Simple and Composite Attributes
𖦹 Simple attributes are indivisible and not divided into subparts.
𖦹 Composite attributes can be divided into subparts (component attributes).
• Example: name = first name + middle initial + last name.
• Example: address = street + city + state + postal code; street can be further divided into street number, street name, apartment number.
- Composite attributes help group related attributes and make modeling cleaner.

188. Single-valued and Multivalued Attributes
𖦹 Single-valued attributes have only one value per entity.
• Example: student ID.
𖦹 Multivalued attributes have multiple values for an entity.
• Example: phone number for instructors; dependent names listing all dependents.

189. Derived Attributes
𖦹 Derived attributes can be computed from other attributes or entities.
• Example: students advised = count of students linked to an instructor.
• Example: age = current date - date of birth.
- Derived attributes are not stored; base attributes are stored.
IMG_URL derived-complex.png

190. Null Values
𖦹 An attribute can take a null value when an entity has no value for it.
• Null may indicate "not applicable", "missing", or "unknown".
• Example: middle initial null if person has no middle name.
• Example: apartment number null if not applicable, missing, or unknown.

191. Mapping Cardinalities
𖦹 Mapping cardinalities describe the number of entities associated via a relationship set.
• Most useful for binary relationship sets but can apply to relationships with more than two entity sets.

192. Types of Mapping Cardinalities
𖦹 One-to-one: Each entity in A associates with at most one entity in B, and vice versa.
𖦹 One-to-many: Each entity in A associates with zero or more entities in B; each entity in B associates with at most one entity in A.
IMG_URL one-to-many.png
𖦹 Many-to-one: Each entity in A associates with at most one entity in B; each entity in B associates with zero or more entities in A.
𖦹 Many-to-many: Each entity in A associates with zero or more entities in B; each entity in B associates with zero or more entities in A.
IMG_URL one-to-many-many.png
- The choice depends on the real-world scenario being modeled.
• Example: Advisor relationship set:
- Many-to-many if students have multiple instructors.
- One-to-many if each student has only one instructor, and instructors advise multiple students.

193. Representation in E-R Diagrams
𖦹 One-to-one: Draw directed lines from the relationship set to both entity sets.
𖦹 One-to-many: Draw directed line to the "one" side; undirected line to the "many" side.
𖦹 Many-to-one: Draw directed line to the "one" side; undirected line to the "many" side.
𖦹 Many-to-many: Draw undirected lines to both entity sets.
IMG_URL one-to-many-to-many-to-one.png

194. Participation Constraints
𖦹 Total participation: Every entity in an entity set must participate in at least one relationship.
𖦹 Partial participation: Some entities may not participate in any relationship.
• Example: Student must have an advisor (total participation); an instructor may have zero or more students (partial participation).
𖦹 Indicated using double lines for total participation in E-R diagrams.
IMG_URL strong-participation.png

195. Cardinality Limits
𖦹 Lines may have minimum and maximum cardinalities in the form l..h.
• Minimum 1 indicates total participation.
• Maximum 1 indicates at most one relationship; maximum ∗ indicates no limit.
• Example: advisor-student line 1..1 = each student has exactly one advisor; advisor-instructor line 0..* = instructor can have zero or more students.
𖦹 Misinterpretation caution: 0..* does not mean many-to-one; it allows multiple relationships on that side.
𖦹 One-to-one occurs if both edges have maximum value 1; 1..* on the left edge implies instructor must advise at least one student.
IMG_URL cardinality.png

196. Nonbinary Relationship Sets
𖦹 Some many-to-one relationships can be specified.
• Example: In a project, a student can have at most one instructor as a guide.
𖦹 Only one arrow out of a nonbinary relationship set is allowed to avoid ambiguity.

197. Primary Key
𖦹 A primary key uniquely distinguishes entities within an entity set and relationships within a relationship set.

198. Entity Sets
𖦹 Individual entities are conceptually distinct and must be uniquely identified using their attributes.
𖦹 No two entities can have identical values for all attributes.
𖦹 Keys for entity sets include superkey, candidate key, and primary key, similar to relation schemas.
𖦹 Keys also help to identify relationships uniquely.

199. Relationship Sets
𖦹 To distinguish relationships in a relationship set R involving entity sets E1, E2, …, En, use the primary keys of the participating entity sets.
• If R has no attributes: primary-key(E1) ∪ primary-key(E2) ∪ … ∪ primary-key(En) forms a superkey.
• If R has attributes a1, a2, …, am: primary-key(E1) ∪ … ∪ primary-key(En) ∪ {a1, …, am} forms a superkey.
𖦹 If primary key attribute names are not unique, rename using entity set name or role name.
𖦹 For binary relationships:
• Many-to-many: union of primary keys from both sides forms primary key.
• One-to-many or many-to-one: primary key of the "many" side is used.
• One-to-one: primary key of either entity set can be used.
𖦹 For nonbinary relationships: only one arrow out is allowed to avoid ambiguity.
• Primary key = union of primary keys of entity sets without incoming arrow.
• If multiple-arrow situations exist, convert nonbinary relationship to entity set or use functional dependencies.

200. Weak Entity Sets
𖦹 Weak entity sets depend on another entity set called the identifying entity set.
𖦹 A weak entity cannot have a primary key alone; use the primary key of the identifying entity set plus discriminator attributes.
𖦹 Every weak entity must be associated with an identifying entity (existence dependent).
𖦹 The identifying relationship is many-to-one from the weak entity set to the identifying entity set and has total participation.
𖦹 Example: section entity is weak, depends on course entity.
• Primary key of section = {course id, sec id, year, semester}.
𖦹 In E-R diagrams:
• Weak entity = double rectangle, discriminator underlined with dashed line.
• Identifying relationship = double diamond.
• Total participation = double lines.
• Arrow points from weak entity to identifying entity.
𖦹 Weak entities can participate in other relationships and can have multiple identifying entity sets.
• Primary key in that case = union of primary keys of identifying entity sets + discriminator.
IMG_URL weak-entity.png

201. Removing Redundant Attributes in Entity Sets
𖦹 In E-R design, after selecting entity sets, choose attributes that capture required values.
𖦹 Redundant attributes occur when attributes in entity sets are repeated in relationship sets.
𖦹 Example: instructor and department
• instructor attributes: ID (primary key), name, dept name, salary
• department attributes: dept name (primary key), building, budget
• Relationship: inst dept relates instructor to department
- dept name is redundant in instructor and should be removed.
𖦹 Removing redundant attributes avoids incorrect assumptions about relationships (e.g., one instructor having only one department).
𖦹 student entity set also does not need dept name, since relationship student dept exists.
𖦹 Example: section and time slot
• section attributes: course id, sec id, semester, year, building, room number, time slot id; primary key = (course id, sec id, year, semester)
• time slot attributes: time slot id (primary key), multivalued composite attribute {(day, start time, end time)}
• Relationship: sec time slot
- time slot id is redundant in section and should be removed.
𖦹 Example: section and classroom
• classroom attributes: building, room number, capacity; primary key = (building, room number)
• Relationship: sec class
- {building, room number} are redundant in section and should be removed.
𖦹 A good E-R design has no redundant attributes.

202. University Example: Entity Sets and Attributes
𖦹 classroom: building, room number, capacity
𖦹 department: dept name, building, budget
𖦹 course: course id, title, credits
𖦹 instructor: ID, name, salary
𖦹 section: course id, sec id, semester, year
𖦹 student: ID, name, tot cred
𖦹 time slot: time slot id, {(day, start time, end time)}

203. University Example: Relationship Sets
𖦹 inst dept: relates instructors to departments
𖦹 stud dept: relates students to departments
𖦹 teaches: relates instructors to sections
𖦹 takes: relates students to sections, with descriptive attribute grade
𖦹 course dept: relates courses to departments
𖦹 sec course: relates sections to courses
𖦹 sec class: relates sections to classrooms
𖦹 sec time slot: relates sections to time slots
𖦹 advisor: relates students to instructors
𖦹 prereq: relates courses to prerequisite courses
𖦹 No entity set has attributes made redundant by relationships.

204. E-R Diagram for University
𖦹 Each instructor must have exactly one department (total participation): double line from instructor to inst dept; arrow to department.
𖦹 Every course must be in some department: double line from course to course dept; arrow to department.
𖦹 Every student must be majoring in a department: double line from student to stud dept; arrow to department.
𖦹 Relationship takes has descriptive attribute grade.
𖦹 Each student has at most one advisor.
𖦹 section is a weak entity set, with sec id, semester, year as discriminator; sec course is identifying relationship linking section to course.
IMG_URL er-university.png

205. Reducing E-R Diagrams to Relational Schemas
𖦹 E-R and relational models are abstract representations of real-world enterprises.
𖦹 Each entity set and relationship set in E-R design corresponds to a unique relation schema.
𖦹 Constraints in E-R design map to constraints in relational schemas.

206. Representation of Strong Entity Sets
𖦹 A strong entity set with simple attributes a1, a2, …, an is represented as a relation schema with n attributes.
𖦹 The primary key of the entity set becomes the primary key of the relation schema.
𖦹 Example: student(ID, name, tot_cred); ID is primary key.
𖦹 Instructor, student, and course schemas differ from previous chapters since dept name is excluded.

207. Representation of Strong Entity Sets with Complex Attributes
𖦹 Composite attributes are split into component attributes; no separate attribute for the composite itself.
• Example: instructor name → first name, middle initial, last name; address → street number, street name, apt number, city, state, postal code.
𖦹 Multivalued attributes require a separate relation schema.
𖦹 Derived attributes are not represented directly but can be implemented as functions or methods.
𖦹 Example: instructor(ID, first name, middle initial, last name, street number, street name, apt number, city, state, postal code, date of birth).
𖦹 Multivalued attribute example: phone number
• instructor phone(ID, phone number)
- Primary key = (ID, phone number); ID references instructor as foreign key.
𖦹 If entity set has only primary key B and multivalued attribute M, relation schema contains B and attribute A corresponding to M.
• Example: time slot(time slot id, day, start time, end time)
- end time may be omitted from primary key for uniqueness.

208. Representation of Weak Entity Sets
𖦹 Weak entity set A with attributes a1, a2, …, am depends on strong entity set B with primary key b1, b2, …, bn.
𖦹 Relation schema for A: {a1, a2, …, am} ∪ {b1, b2, …, bn}.
𖦹 Primary key = combination of discriminator of A and primary key of B.
𖦹 Foreign key: attributes b1, b2, …, bn reference B's primary key.
𖦹 Example: section(course id, sec id, semester, year); course id references course.

209. Representation of Relationship Sets
𖦹 Relationship set R with entity primary keys {a1, …, am} and descriptive attributes {b1, …, bn} is represented by schema {a1, …, am} ∪ {b1, …, bn}.
𖦹 Primary key selection follows binary relationship rules.
𖦹 Foreign-key constraints: attributes from entity sets reference their respective schemas.
𖦹 Example: advisor relationship between instructor(ID) and student(ID)
• Schema: advisor(s ID, i ID)
• Primary key = s ID; foreign keys: s ID → student, i ID → instructor
𖦹 Role indicators used as attribute names when entity sets repeat, e.g., prereq(course id, prereq id).
𖦹 Many-to-one relationships: primary key from one related entity set used.

210. Redundancy of Schemas
𖦹 Relationship sets linking weak entity sets to strong entity sets often produce redundant schemas.
𖦹 Example: sec course schema duplicates section schema attributes; can be eliminated.

211. Combination of Schemas
𖦹 Many-to-one relationship AB with total participation of A allows combining A and AB schemas.
• Example: inst dept → combine with instructor → instructor(ID, name, dept name, salary)
• stud dept → combine with student → student(ID, name, dept name, tot cred)
• course dept → combine with course → course(course id, title, dept name, credits)
• sec class → combine with section → section(course id, sec id, semester, year, building, room number)
• sec time slot → combine with section → section(course id, sec id, semester, year, building, room number, time slot id)
𖦹 One-to-one relationships can combine with either entity set.
𖦹 Partial participation uses nulls for missing attributes.
𖦹 Foreign-key constraints referencing the merged entity set are enforced implicitly; others remain in combined schema.

212. Extended E-R Features  
𖦹 Some database aspects are better expressed using extended E-R features: specialization, generalization, higher- and lower-level entity sets, attribute inheritance, and aggregation.  
𖦹 Example: A university database defines a person entity set with attributes ID, name, street, and city.

213. Specialization  
𖦹 Specialization designates subgroupings within an entity set based on distinctive features.  
• Example: person can be specialized into employee and student.  
- Employee may have salary, student may have tot cred.  
𖦹 Specialization can be applied repeatedly.  
• Example: student can be specialized into graduate (office number) and undergraduate (residential college).  
• Example: employee can be specialized into instructor (rank) and secretary (hours per week).  
𖦹 An entity may belong to multiple specializations.  
• Example: a temporary employee who is a secretary.  
𖦹 Depicted in E-R diagram using hollow arrow-head (ISA relationship).  
𖦹 Specialization types:  
• Overlapping: entity may belong to multiple specialized sets.  
• Disjoint: entity may belong to at most one specialized set.  

214. Generalization  
𖦹 Generalization synthesizes multiple lower-level entity sets into a higher-level entity set based on common features.  
• Example: instructor and secretary share common attributes (ID, name, salary) → generalized into employee.  
𖦹 Generalization is the inverse of specialization.  
𖦹 Higher-level entity set is called superclass; lower-level sets are subclasses.  
𖦹 Emphasizes similarities among lower-level sets and reduces attribute repetition.
IMG_URL spec-gen.png

215. Attribute Inheritance  
𖦹 Lower-level entity sets inherit attributes of higher-level sets.  
• Example: student and employee inherit ID, name, street, city from person.  
• Example: instructor and secretary inherit attributes from employee and person.  
𖦹 Lower-level entity sets also inherit participation in relationships of higher-level sets.  
• Example: if person participates in person dept with department, all subclasses also participate.  
𖦹 Outcome:  
• Higher-level set has common attributes and relationships.  
• Lower-level sets have distinctive features.  
𖦹 Single inheritance: entity set is a lower-level in only one ISA relationship.  
𖦹 Multiple inheritance: entity set is a lower-level in multiple ISA relationships → forms a lattice.
IMG_URL spec-gen.png

216. Constraints on Specializations  
𖦹 Specializations may have disjointness and completeness constraints.  
• Total specialization: each higher-level entity belongs to at least one lower-level set.  
• Partial specialization: some higher-level entities may not belong to any lower-level set.  
𖦹 Partial specialization is default.  
𖦹 Total specialization represented in E-R diagram using keyword “total” and dashed line to hollow arrowhead.  
𖦹 Constraints affect insertions/deletions:  
• In total specialization, inserting an entity in higher-level set requires insertion in a lower-level set.  
• Deleting from higher-level set requires deletion from all associated lower-level sets.  

217. Aggregation  
𖦹 Aggregation treats relationships as higher-level entities to express relationships among relationships.  
• Example: proj guide (instructor, student, project) → evaluation entity related via eval for relationship.  
IMG_URL wo-aggregation.png
𖦹 Avoid combining proj guide and eval for into single relationship due to possible missing evaluations.  
𖦹 Aggregation allows modeling eval for as a relationship between proj guide (as higher-level entity) and evaluation.
IMG_URL w-aggregation.png

218. Reduction to Relation Schemas - Generalization  
𖦹 Method 1: Create schema for higher-level set and each lower-level set.  
• Include lower-level attributes plus primary key attributes of higher-level set.  
• Create foreign-key constraints from lower-level sets to higher-level set.  
• Example:  
person (ID, name, street, city)  
employee (ID, salary)  
student (ID, tot cred)  
𖦹 Method 2: If generalization is disjoint and complete, do not create schema for higher-level set.  
• Include higher-level attributes in each lower-level schema.  
• Example:  
employee (ID, name, street, city, salary)  
student (ID, name, street, city, tot cred)  
𖦹 Method 2 drawbacks:  
• Foreign-key constraints on higher-level set become difficult.  
• Overlapping generalizations may store duplicate values.  
• Incomplete generalization requires extra higher-level schema.

219. Reduction to Relation Schemas - Aggregation  
𖦹 Create schema for relationship set involving aggregation.  
• Include primary key attributes from aggregated relationship and related entity sets.  
• Include any descriptive attributes.  
𖦹 Treat aggregation as any other entity set.  
𖦹 Primary key of aggregation is primary key of defining relationship set.  
𖦹 No separate relation is required; use relation created from defining relationship.

220. Entity-Relationship Design Issues
𖦹 Entity sets and relationship sets are not precisely defined.
𖦹 Multiple ways exist to define entities and relationships.
IMG_URL erroneous.png

221. Common Mistakes in E-R Diagrams
𖦹 Using primary key of one entity as an attribute in another entity instead of a relationship.
• Example: dept_name as an attribute of student is incorrect; use stud_dept relationship instead.
- Avoids duplication and keeps relationships explicit.
𖦹 Using primary-key attributes of related entities as attributes of the relationship set.
• Example: ID of student and instructor should not be attributes of advisor relationship.
𖦹 Using single-valued attributes in relationships requiring multivalued attributes.
• Example: Adding assignment and marks to takes relationship only allows one assignment per student-section pair.
- Correct alternatives: model assignment as weak entity with marks relationship or use multivalued composite attribute {assignment_marks} in takes.
𖦹 Break large E-R diagrams into pieces; show attributes of entity set only in first occurrence.
IMG_URL correct-one.png

222. Use of Entity Sets versus Attributes
𖦹 Consider instructor with phone_number attribute.
𖦹 Phone can be modeled as:
• Attribute phone_number of instructor (one phone per instructor).
• Entity set phone with attributes phone_number and location.
• Relationship inst_phone between instructor and phone.
𖦹 Treating phone as entity allows multiple phones and extra information (type, location, shared users).
𖦹 Attribute like name should remain an attribute, not an entity.
𖦹 Decision depends on real-world semantics.
IMG_URL entity-sets-vs-attributes.png

223. Use of Entity Sets versus Relationship Sets
𖦹 Decide whether to model as entity set or relationship set.
• Example: replaces takes relationship by registration entity set with section_reg and student_reg relationships.
𖦹 Use relationship set to describe actions between entities.
𖦹 Some attributes may be better expressed as relationships.
IMG_URL entity-sets-vs-relationship-sets.png

224. Binary versus n-ary Relationship Sets
𖦹 Binary relationships are common; n-ary can often be replaced by multiple binary relationships.
• Example: ternary parent relationship can be split into mother and father binary relationships.
𖦹 For abstract n-ary relationship R between A, B, C:
• Create entity set E representing R.
• Create binary relationships RA, RB, RC from E to A, B, C.
• Assign any attributes of R to E.
𖦹 This increases complexity and storage.
𖦹 N-ary relationships show participation more clearly.
𖦹 Some constraints may not translate to binary relationships.
• Example: proj_guide cannot be split naturally without losing pairing information.
IMG_URL binary-vs-nary.png

225. Alternative Notations for Modeling Data
𖦹 Diagrammatic representation helps communication between data modelers and domain experts.
𖦹 Common notations: E-R diagrams and UML class diagrams.
𖦹 No universal standard; different books and tools use different notations.
𖦹 Symbols used: entity set, relationship set, weak entity, primary key, cardinality, attributes (simple, composite, multivalued, derived), ISA generalization, total/disjoint participation, role indicator.
IMG_URL symbols-in-er.png

226. Alternative E-R Notations
𖦹 Attributes can be shown in ovals; primary keys underlined.
𖦹 Relationship attributes connected to diamonds.
𖦹 Cardinality: ∗ and 1, or crow’s-foot notation; total participation with vertical bar, partial with circle.
𖦹 Generalization represented with triangles.
𖦹 Chen’s notation uses ovals and diamonds.
𖦹 IDEF1X standard (1993) uses crow’s-foot, vertical bars for total participation, hollow circles for partial.
𖦹 Modern notation aligns with UML class diagrams; compact and tool-friendly.
𖦹 Some tools allow entity view (without relationship attributes) or relational view (with attributes).
IMG_URL alternative-notations.png

227. The Unified Modeling Language (UML)
𖦹 UML models various software components beyond data: user interactions, functional modules.
𖦹 Key diagrams:
• Class diagram: similar to E-R diagrams, shows attributes and methods.
• Use case diagram: user-system interactions.
• Activity diagram: flow of tasks.
• Implementation diagram: system components and interconnections.
𖦹 UML objects have attributes and methods; access: + public, - private, # protected.
𖦹 Binary relationships shown as lines; nonbinary relationships via diamond notation.
𖦹 Cardinality constraints l..h; placement reversed from E-R diagrams.
𖦹 Supports generalization, disjoint/overlapping, composition (existence dependent) and aggregation (may exist independently).
𖦹 UML does not support composite/multivalued attributes; derived attributes map to methods.
𖦹 Interfaces and object-oriented features represented in UML class diagrams.
IMG_URL er-uml.png

228. Other Aspects of Database Design
𖦹 Database design is not only schema design.
𖦹 Functional requirements:
• Define transactions and queries.
• Plan user interfaces.
• Implement authorization at database or application level.
𖦹 Data flow and workflow:
• Workflows combine tasks and data.
• Interact with database; track tasks and routing.
• Helps understand business processes for database design.
𖦹 Schema evolution:
• Database needs evolve over time; schema may require updates.
• Distinguish fundamental constraints (permanent) vs. changeable constraints.
• Good design anticipates future changes to minimize modifications.
𖦹 Database design is human-oriented:
• End users are people.
• Designers interact with domain experts to understand requirements.

SOURCE: “Database Management Systems” by Raghu Ramakrishnan and Johannes Gehrke, McGraw-Hill
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -[12/11/2025]
1. File organization and basic data access
𖦹 A collection of records is stored in files, and each file consists of pages.
𖦹 The file layer arranges data to support fast access to needed records.
𖦹 A file organization is a way of arranging records on disk.
• Each organization makes some operations efficient and others slower.
𖦹 Example: Sorting employee records by age makes age-based retrieval fast.
• But maintaining sorted order is expensive when records change.
𖦹 If we want to retrieve employees with salary > 5000, we must scan the whole file.
𖦹 Indexing helps support multiple query types efficiently.
𖦹 Choosing the right file organization affects performance.
𖦹 Choosing good indexes is one of the most effective performance improvements.

2. Data on external storage
𖦹 Database data must persist across program executions, so it is stored on external storage.
𖦹 Data is stored on disks or tapes and brought into memory when needed.
𖦹 A page is the unit of data transfer between disk and memory, often 4KB or 8KB.
𖦹 Page I/O cost dominates the total cost of operations, so DBMS optimizes to reduce it.
𖦹 Disks allow random page access at a roughly fixed cost.
• Reading pages sequentially is faster than reading them in random order.
𖦹 Tapes allow only sequential access and are mainly used for archival storage.
𖦹 Every record has a unique record id (rid).
• Using rid, the system can locate the disk page containing the record.

3. Buffer manager and disk space manager
𖦹 The buffer manager handles moving pages between disk and memory.
• When a page is needed, the file layer requests it from the buffer manager.
𖦹 The disk space manager allocates and tracks disk pages.
• When new space is needed, it allocates pages.
• When pages are no longer needed, it marks them free and reuses them later.
𖦹 The files and access methods layer relies on these managers for storage and retrieval.

4. File organizations and indexing
𖦹 A file of records is handled by the files and access methods layer.
𖦹 The file layer can create, delete, insert records, delete records, and scan through all records.
• A relation is stored as a file of records.
𖦹 Records are stored across disk pages, and the file layer tracks allocated pages and available space.
𖦹 The simplest file organization is a heap file where records are stored in random order.
𖦹 An index is a data structure that supports fast retrieval based on search key fields.
• Additional indexes can be created on different search keys to support different search operations.

5. Data entries in an index
𖦹 A data entry (k*) in an index contains enough information to locate records with search key value k.
𖦹 Searching the index finds data entries, which are then used to locate data records.
𖦹 Three alternatives for storing data entries:
• Alternative (1): The data entry is the actual data record.
• Alternative (2): The data entry is a (k, rid) pair, where rid is the record id.
• Alternative (3): The data entry is a (k, rid-list) pair, where rid-list contains all matching record ids.
𖦹 Alternative (1) means the index itself is a file of data records.
𖦹 Alternatives (2) and (3) store pointers and are independent of how the main data file is organized.
𖦹 Alternative (3) uses space efficiently but has variable-length entries.
𖦹 Only one index should use Alternative (1) to avoid storing records multiple times.

6. Clustered indexes
𖦹 An index is clustered if the order of data records matches the index order.
𖦹 If the index uses Alternative (1), it is clustered by definition.
𖦹 Alternatives (2) and (3) are clustered only if the data file itself is sorted by the search key.
𖦹 In practice, files are rarely kept sorted, so clustered indexes usually use Alternative (1).
𖦹 A clustered index returns data pages that are close together for range queries, reducing page I/O.
𖦹 An unclustered index may require many separate page fetches for range queries.

7. Primary and secondary indexes
𖦹 An index on fields that include the primary key is called a primary index.
𖦹 Indexes on other fields are secondary indexes.
𖦹 Terminology varies; some define primary index as Alternative (1) and secondary as Alternatives (2) or (3).
𖦹 A primary index does not have duplicate search key values.
𖦹 Secondary indexes may contain duplicates.
𖦹 If no duplicates exist, the index is called a unique index.
IMG_URL primary-secondary-indices.png

8. Index data structures
𖦹 Data entries can be organized using hashing or tree-based structures.
𖦹 Either hashing or tree indexing can be used with any of the three data entry alternatives.

9. Hash-based indexing
𖦹 Hashing groups records into buckets based on a hash function applied to the search key.
𖦹 Each bucket consists of a primary page and possibly overflow pages linked in a chain.
𖦹 Searching:
• Apply the hash function to identify the correct bucket.
• Retrieve all pages in that bucket to find matching records.
𖦹 Insertion:
• Insert the record into its bucket.
• Overflow pages are added if the bucket is full.
𖦹 If we do not know the search key value, we must scan the entire file.
𖦹 Hash-based indexes usually allow retrieval of a page in one to two disk I/Os.
𖦹 Example:
• File of employees hashed on age uses Alternative (1), storing actual data records.
• Auxiliary index on sal uses Alternative (2), storing (sal, rid) pairs.
IMG_URL hash-index.png

10. Tree-based indexing
𖦹 Data entries are stored in sorted order by search key.
𖦹 A hierarchical tree structure guides searches to the correct leaf page.
𖦹 Leaf level contains the data entries.
𖦹 Non-leaf nodes contain search key values and pointers to child nodes.
• Pointer to the left of key k leads to entries less than k.
• Pointer to the right of key k leads to entries greater than or equal to k.
𖦹 Leaf pages are linked to allow efficient scanning across a range of values.
𖦹 The search cost equals the height of the tree plus the number of leaf pages that satisfy the condition.
𖦹 B+ tree maintains equal path length from root to every leaf, ensuring balance.
𖦹 Tree height is usually small (often 3 or 4), so retrieval typically takes only a few disk I/Os.
𖦹 Fan-out refers to the average number of children of a non-leaf node.
• If fan-out is F and height is h, number of leaf pages is approximately F^h.
𖦹 With large fan-out (e.g., F ≥ 100), a tree of height 4 can index around 100 million leaf pages efficiently.
IMG_URL tree-index.png

11. Fan-out and tree height
𖦹 The average number of children for a non-leaf node is called the fan-out.
𖦹 If every non-leaf node has n children, a tree of height h has n^h leaf pages.
𖦹 In practice, nodes have varying numbers of children, but the average F can approximate leaf pages as F^h.
𖦹 Typically, F is at least 100, so a tree of height 4 can have 100 million leaf pages.
𖦹 Searching such a tree requires 4 I/Os.
• This is much faster than a binary search, which would require over 25 I/Os for the same number of pages.

12. Comparison of File Organizations
𖦹 Basic file organizations considered:
• Heap file (randomly ordered records)
• Sorted file (records sorted on age, sal)
• Clustered B+ tree file (search key age, sal)
• Heap file with unclustered B+ tree index (age, sal)
• Heap file with unclustered hash index (age, sal)
- Choice of file organization affects operation costs. Sorting or indexing can improve performance for some operations.

13. Operations on Files
𖦹 Scan: Fetch all records from disk into memory.
𖦹 Search with Equality Selection: Fetch records matching exact values (e.g., age=23, sal=50).
𖦹 Search with Range Selection: Fetch records in a range (e.g., age>35).
𖦹 Insert: Find appropriate page, add record, write back, may affect other pages.
𖦹 Delete: Locate page and record, remove record, write back, may affect other pages.

14. Cost Model
𖦹 B = number of data pages, R = records per page.
𖦹 D = average time to read/write a page, C = average time to process a record.
𖦹 H = time to apply hash function, F = fan-out for tree indexes.
𖦹 I/O usually dominates cost; CPU is less significant.
𖦹 Simplified model counts pages read/written; ignores blocked access and other factors.

15. Heap Files
𖦹 Scan cost: B(D + RC)
𖦹 Equality selection (candidate key): 0.5B(D + RC)
𖦹 Equality selection (non-key): B(D + RC)
𖦹 Range selection: B(D + RC)
𖦹 Insert at end: 2D + C
𖦹 Delete by rid: D + C + D
- Deletion by condition may require scanning and modifying multiple pages.

16. Sorted Files
𖦹 Scan cost: B(D + RC)
𖦹 Equality selection matching sort order: Dlog2B + Clog2R
• Multiple matching records are contiguous; sequential retrieval is efficient.
𖦹 Range selection: Search first record (Dlog2B + Clog2R), then sequentially fetch pages.
𖦹 Insert: Search + read/write subsequent pages; cost ~ search + B(D + RC)
𖦹 Delete: Search + read/write subsequent pages to compact; same cost as insert.
- Sorting helps locate records efficiently but increases insertion/deletion cost.

17. Clustered Files
𖦹 Average page occupancy: 67%, total pages ~1.5B
𖦹 Scan: 1.5B(D + RC)
𖦹 Equality selection: DlogF1.5B + Clog2R
𖦹 Range selection: First record located as equality, then sequentially fetch leaf pages.
𖦹 Insert: Search + write modified leaf page; occasional splits ignored
𖦹 Delete: Search + modify/write pages; similar to insert
- Clustered files combine sorted order benefits with efficient insert/delete.

18. Heap File with Unclustered Tree Index
𖦹 Each data entry ~0.1 size of a record; leaf pages ~0.15B
𖦹 Scan: Leaf level cost 0.15B(D + 6.7RC) + fetch each record BR(D + C)
𖦹 Equality search: DlogF0.15B + Clog2(6.7R) + D
𖦹 Range search: Cost depends on number of qualifying records; each requires one I/O
𖦹 Insert: 2D + C (heap) + DlogF0.15B + Clog2(6.7R) + D (index)
𖦹 Delete: Locate record + index, then write modified pages; cost 2D + DlogF0.15B + Clog2(6.7R)
- Unclustered index fast for single-record search but expensive for scans/range queries.

19. Heap File with Unclustered Hash Index
𖦹 Each data entry ~0.1 size of record; pages ~0.125B; 80% occupancy
𖦹 Scan: Retrieve entries 0.125B(D + 8RC) + fetch each record BR(D + C), very expensive
𖦹 Equality search: H + 2D + 4RC
𖦹 Range search: Entire heap file scanned; cost B(D + RC)
𖦹 Insert: 2D + C (heap) + H + 2D + C (index)
𖦹 Delete: Locate record + index: H + 2D + 4RC, then write pages 2D
- Hash indexes are very fast for equality, do not support efficient range searches.

20. Comparison of I/O Costs
𖦹 Heap file: efficient storage, fast scan/insert, slow search/delete
𖦹 Sorted file: efficient storage, fast search, slow insert/delete
𖦹 Clustered file: fast search, efficient insert/delete, extra space overhead
𖦹 Unclustered tree/hash: fast search/insert/delete, slow scan/range search
𖦹 Hash slightly faster for equality, cannot do range search
- No single file organization is best for all operations.

21. INDEXES AND PERFORMANCE TUNING
𖦹 Indexes improve database performance and must be chosen based on expected workload.
𖦹 Index choice impacts performance and requires understanding query evaluation and concurrency control.
𖦹 Examples with multiple tables require knowledge of join algorithms and query plans.

22. Impact of the Workload
𖦹 Consider expected workload and common operations when choosing indexes.
𖦹 Indexes support efficient retrieval for selection conditions: equality and range.
• Hash-based indexes are good for equality selections but poor for range selections.
• Tree-based indexes support both equality and range selections efficiently.
𖦹 Both tree and hash indexes handle inserts, deletes, and updates efficiently.
𖦹 Tree-based indexes are better than fully sorted files for:
• Efficient inserts and deletes.
• Faster leaf page search than binary search in sorted files.
𖦹 Sorted files are faster for sequential page retrieval but expensive for inserts and deletes.
𖦹 ISAM combines sequential leaf allocation with fast search; inserts/deletes are better than sorted files but worse than B+ trees.

23. Clustered Index Organization
𖦹 Clustered index organizes the underlying data records; only one clustered index per table.
𖦹 Multiple unclustered indexes can exist on a data file.
𖦹 Clustered indexes are expensive to maintain when inserting into full leaf pages due to record movement and updates in other indexes.
𖦹 Clustering should be used sparingly for queries that benefit from sequential access.
𖦹 Hash indexes are unsuitable for clustered files because range queries cannot use them.
𖦹 Index-only evaluation can compute queries using just index entries without fetching data records.
𖦹 Example: Using a B+ tree index on age for SELECT <pink>FROM</pink> <yellow>Employees E</yellow> <pink>WHERE</pink> <yellow>E.age > 40</yellow>.
• Benefit depends on selectivity of the condition and whether the index is clustered.
𖦹 Queries on non-key fields may benefit from clustering if many records share the same value.
𖦹 Aggregate queries can use indexes (clustered or unclustered) without accessing full tuples.

24. Composite Search Keys
𖦹 A composite search key contains multiple fields, e.g., (age, sal) or (sal, age).
𖦹 Equality queries bind all fields in the search key to constants; hash indexes support only equality queries.
𖦹 Range queries bind only some fields; tree indexes support range queries on a prefix of the composite key.
𖦹 Composite keys allow broader query matching and increase opportunities for index-only evaluation.
𖦹 Drawbacks: updates affect any field in the key; composite indexes are larger and may increase tree levels.
𖦹 Design examples:
• SELECT <pink>FROM</pink> <yellow>Employees E</yellow> <pink>WHERE</pink> <yellow>E.age BETWEEN 20 AND 30 AND E.sal BETWEEN 3000 AND 5000</yellow>
- A composite B+ tree index on (age, sal) is effective; hash index cannot help.
• SELECT <pink>FROM</pink> <yellow>Employees E</yellow> <pink>WHERE</pink> <yellow>E.age = 25 AND E.sal BETWEEN 3000 AND 5000</yellow>
- Index on (age, sal) performs better than (sal, age) due to ordering of attributes.
• Aggregate queries benefit from composite indexes for index-only scans.

25. Index Specification in SQL: 1999
𖦹 SQL:1999 does not require support for indexes or provide standard commands to create/drop them.
𖦹 Commercial DBMS support indexes; example for creating a B+ tree index:
<pink>CREATE INDEX</pink> <yellow>IndAgeRating ON Students</yellow>
<pink>WITH STRUCTURE =</pink> <yellow>BTREE, KEY = (age, gpa)</yellow>
𖦹 The index automatically updates with inserts and deletes on the Students table.
